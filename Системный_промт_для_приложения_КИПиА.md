# Системный промт для ИИ-ассистента — БОЕВОЙ репозиторий kip8

> **Версия документа:** 2026-09-03 (post-Task 306: ПЕРЕНОС ПАРТИИ Tasks 298-306 из kip8test@4fc48bc в боевой kip8 одним инкрементом SW kipia-v410→v411 — по указанию пользователя «Перенеси все последние изменения в боевой kip8». ПАРТИЯ: (Task 298) КОДЫ СТАТУСОВ Т-12/Т-13 — состав 10 → 16 кодов (Д8/Д7,2 для 8/7,2-час смен дневного персонала; строчные д/н — работа в выходные/праздники; У — учебный отпуск; ПР — прогул; «.» — плановый выходной; ОТ — отпуск, ОВ — отгул, «*» — примечание), регистрозависимые; сервер generateMonth пишет «ОТ» вместо «О» (шаг 4.5); НОВЫЙ _validateStatusCode — setManualEntry валидирует код по листу «Коды_статусов» (fail-closed: unknown_статус); клиент: fallback 16 кодов, динамический select шита правки (_fillStatusSelect из справочника), план отпуска в пустых ячейках «ОТ» + тултип. (Task 299) scripts/StatusCodesInit.gs — одноразовый GAS-инструмент замены листа «Коды_статусов» на 16 эталонных кодов + миграция «О»→«ОТ» (ВЫПОЛНЕН в kip8test — повторно НЕ запускать; updateCycleDaysTemplate2 НЕ запускать). (Task 302) цвет Д7,2 = Д8 #FFF9C4 (fallback index.html + эталон SC_STATUS_CODES). (Task 303) СЛОЙ МЕРОПРИЯТИЙ И/ОБ/ПЗ — приоритет статуса дня «ручная > отпуск > плановая смена > мероприятие» (мероприятие больше НЕ затирает смену «Д8»→«И»); И/ОБ/ПЗ пишутся только на дни БЕЗ смены; НОВЫЙ шаг 4.6 generateMonth — сверка устаревших строк мероприятий (restore/delete, идемпотентность); бейдж .ws-ev-badge в правом нижнем углу ячейки (цвет справочника, пунктирный ws-ev-pending на пустой, скрыт на днях отсутствия); попап ячейки — секция «Мероприятия в этот день» + «+ Мероприятие…» с префиллом. (Task 304) ТАБ_№ ХРАНИТСЯ ТЕКСТОМ — ведущие нули «0871» не теряются (баг USER_ENTERED-семантики appendRow: бейдж мероприятия молча пропадал): НОВЫЙ хелпер _appendRowKeepText (setNumberFormat «@» ДО записи) во всех 4 путях записи таб_№ — addEmployee/addTraining/addVacation/setManualEntry + «@» на диапазон вставки в generateMonth; НОВОЕ поле warnings[] (таб не из справочника → тост «⚠ … не найден в «Сотрудниках»»); scripts/TabNumbersFix.gs — разовая починка испорченных ячеек (выполнен в kip8test). (Task 305) ДНИ ОТПУСКА — код отпуска ОСНОВНЫМ значением ячейки, плановая смена по циклу (Дни_цикла, алгоритм шага 3 generateMonth = _plannedShiftAt) — бейджем .ws-ev-badge.ws-ev-shift в правом нижнем углу в формате мероприятия (цвет справочника: Д #FFE082, Н #B0BEC5, Д8/Д7,2 #FFF9C4); «отрисовка, не хранение» — Apps Script не менялся, работает и ДО «Сформировать» (план «ОТ» пунктиром + сплошной бейдж). (Task 306) ТРИ ПРАВКИ: 1) бейдж плановой смены — ТОЛЬКО у сотрудников типа «сменный» (empIsShift = String(emp[«тип»]).trim() === «сменный» в _renderCell; дневные Д8/Д7,2 и без типа — без бейджа; расчёт _plannedShiftAt НЕ тронут — паритет с сервером сохранён); 2) мероприятия + «прогул» → ПР и «примечание» → «*»: _EVENT_CODES [«И»,«ОБ»,«ПЗ»,«ПР»,«*»], форма «Новое мероприятие» 5 типов, сервер TRAINING_TYPE_TO_STATUS + прогул/примечание (неизвестный тип → invalid_тип), _isEventStatusCode + ПР/* (авто-строки на дни без смены, бейдж на сменных; шаг 4.6 снимает устаревшие; РУЧНЫЕ ПР/* не трогаются; ПР остаётся в _ABSENCE_CODES — день-прогул = день отсутствия); плашки .ws-type-ПР #EF5350 / .ws-type-* #FFAB91; 3) КНОПКИ «Обновить»+«Сформировать» ОБЪЕДИНЕНЫ: wsCalChip + ProdCalendar.confirmRefresh + мёртвый CSS удалены; подтверждённое формирование тихо обновляет производственный календарь ПАРАЛЛЕЛЬНО генерации (_refreshProdCalendarQuiet → ProdCalendar.refreshNow(true) — silent, сбой не мешает формированию); вызовы из _doGenerateMonth/_doGenerateYear. ПЕРЕНЕСЕНО: index.html (патч 38 хунков 3bf42a4..4fc48bc, все с offset −15 — изоляция не затронута; верификация: diff(kip8test@4fc48bc, kip8-новый) == базовый изоляционный дифф Task 292 по СОДЕРЖИТЕЛЬНОЙ части (82 строки, только сдвиг номеров +473) — байт-в-байт), sw.js v410→v411 (+комментарий партии в шапке), scripts/WorkSchedule.gs (копия байт-в-байт — Tasks 298/303/304/306: _validateStatusCode, мероприятия/шаг 4.6, _appendRowKeepText + warnings, прогул/примечание), scripts/StatusCodesInit.gs + scripts/TabNumbersFix.gs (новые справочные копии), tests/ — 12 файлов (4 новых: test-work-events/test-tab-numbers/test-vacation-shift/test-task306 + 8 обновлённых; SW-ассерты kipia-test-v545→kipia-v411, негативные — kipia-v410; run-all +4 require). Тесты: 1433 passed / 0 failed (паритет с kip8test@4fc48bc). Верификация: node --check sw.js; scripts/task306-browser-check.py (порт 8931, скриншоты /tmp) — 29/29 PASS (бейжд плановой смены только у «сменного» 017; бейдж «ПР» #EF5350 на смене «Н»; пунктирный «*» на пустой ячейке; дневной 023 — событие только в тултипе; форма 5 типов; в тулбаре НЕТ «Обновить», «Сформировать» одна; клик → диалог → месяц = 1 вызов generateMonth + ТИХОЕ обновление календаря (spy silent=true); год = 12 вызовов; окошко норм живо; 0 JS-ошибок; localStorage БЕЗ префикса kip8test:). АКТУАЛИЗАЦИЯ: 93 страницы, ~39.8 тыс. строк / 2.46 MB, тесты 1433 (24 тест-файла), позиции модулей KipAuth ~28 170 / KipFav ~29 724 / KipCableJournal ~31 082 / FlowmeterData ~32 932, WEB_APP_URL ~28 176, пример CACHE_VERSION kipia-v411. ДЕПЛОЙ ПОЛЬЗОВАТЕЛЯ: Apps Script — WorkSchedule.gs заменить + «Новая версия» у развёртывания AKfycbyt… (Tasks 298/303/304/306 меняли сервер; ОДИН бэкенд на оба сайта — если уже сделано при деплое kip8test, НЕ повторять); фронтенд — GitHub Pages подхватит сам (1–3 мин после push), затем Ctrl+Shift+R ×1–2 (SW v410→v411); см. scripts/DEPLOY-Task306-transfer.md. Последнее изменение КОДА: Task 306 — партия 298-306 (документация post-Task 298 — см. предыдущую запись в строке 4).)
> **Версия документа (предыдущая):** 2026-09-02 (post-Task 298: ПАРИТЕТ ДОКУМЕНТАЦИИ с kip8test/Task 297 — устаревшие места промта kip8 выровнены с фактическим состоянием репозитория после переноса Tasks 293-296; КОД НЕ МЕНЯЛСЯ, SW kipia-v410 без изменений, тесты 1324/0 (проверено прогоном), деплой не нужен — docs-only. ИСПРАВЛЕНО: (1) «Источники проекта» — Apps Script URL был СТАРЫМ (AKfycbztm… — ловушка двух развёртываний Task 284): заменён на развёртывание пользователя AKfycbyt…/exec (сверено с KipAuth.WEB_APP_URL в index.html ~строка 28 129), подпись «ОДИН бэкенд на kip8 и kip8test + матрица прав»; (2) «Источники данных» — users/sessions/audit_log отнесены к таблице KIP8_Access + строка «МАТРИЦА ПРАВ» (листы matrix/permissions/roles, 13 прав × 12 ролей, ID 1TmmNZLU…); (3) секция Apps Script — в состав файлов добавлены WorkSchedule.gs / ValidationRules.gs / VacationsInit/VacationsDiagnose / RoleMatrix.gs / RoleMatrixGate.gs / RoleMatrixInit.gs; WEB_APP_URL ~строка 18 630 → ~28 129; эндпоинты дополнены: getMyAccess (карта прав по матрице, Task 295), adminCreateUser, flowmeter.updatePeriodReading/setComment/getValidationRules/getRecentAllMeters, workSchedule.* (шахматка/сотрудники/инструктажи/отпуска, Tasks 201-275); (4) архитектура auth — в диаграмму добавлены RoleMatrix.gs + RoleMatrixGate.gs (гейт rmRequirePerm ДО модулей) + WorkSchedule.gs, хранилище → KIP8_Access (users/sessions/audit_log/matrix/permissions/roles), примечание Task 294-296 (сервер гейтит каждый защищённый action, клиент строит видимость по getMyAccess, кэш kip8_my_access); (5) localStorage-таблица — +ключ kip8_my_access (кэш карты прав, Task 296, БЕЗ префикса); (6) «Роли и доступ» — примечания «легаси-карта» для «Полной матрицы (11 фильтров)» и READ_ROLES/INPUT_ROLES (основная проверка — flowmeter.view/flowmeter.input из матрицы через _requireRead/_requireEdit); (7) «Перенос в kip8» — строка scripts/*.gs (справочные копии серверного кода; ОДИН бэкенд: заменил файлы в редакторе → «Новая версия» AKfycbyt… — один деплой на оба сайта) + последняя принятая партия Tasks 293-296 из kip8test@3bf42a4; (8) АКТУАЛИЗАЦИЯ СЧЁТЧИКОВ: 93 страницы (было «86+»/«92+»), ~39 тыс. строк / ~2.4 MB (было «~27-30 тыс. / ~1.7 MB»), тесты 1324 (было «498»/«872»/«292»), позиции модулей KipAuth ~28 123 / KipFav ~29 677 / KipCableJournal ~31 035 / FlowmeterData ~32 885 (были «~18 628-19 616» и др.), пример CACHE_VERSION kipia-test-v455 → kipia-v410; контекст — «Email+OTP с матрицей прав KIP8_Access». Последнее изменение КОДА: Task 296 — партия 293-296, kipia-v410 (см. предыдущую запись в строке 4).) ; 2026-09-02 (post-Task 296: перенос партии Tasks 293-296 из kip8test@3bf42a4 одним инкрементом SW kipia-v409→v410 — СИСТЕМА ДОСТУПА ПО МАТРИЦЕ KIP8_Access (Google-таблица 1TmmNZLU…, листы matrix/permissions/roles, 13 прав × 12 ролей; пользователь подтвердил работу в kip8test — «проверил несколько прав, работает»). ЭТАПЫ: (Task 293/Этап 1) scripts/RoleMatrixInit.gs — одноразовый init листов (закрепление ТОЛЬКО строк — merge через закреплённые столбцы отравляет лист; _mergeRow, _verifyCreated самопроверка чтением, roleMatrixStatus; roleMatrixCleanup НЕ запускать — удалит живые данные); (Task 294/Этап 2) scripts/RoleMatrix.gs — roleMatrixGetAccess(role)/roleMatrixHasPermission/roleMatrixGetAccessForEmail/roleMatrixInvalidateCache/roleMatrixDebug: ДИНАМИКА (границы по контенту — листы несут стилевой холст A1:Z1000, getLastRow()=1000), FAIL-CLOSED (никогда не бросает исключений; роль/email не найдены → доступ закрыт), Админ — все права матрицы кроме режимного kipios.restricted, admin.panel неотключаем; кэш CacheService TTL 300с; (Task 295/Этап 3) scripts/RoleMatrixGate.gs — rmRequirePerm(token, perm, action) гейт ДО вызова модулей (fail-closed, каждый отказ — аудит RM_ACCESS_DENIED в лог), rmGetMyAccess(token) — карта прав для клиента, rmGateStatus — диагностика; Code.gs — GATE_ACTIONS (admin*→admin.panel, cableJournal.записи→cablejournal.edit) + case getMyAccess + защита от частичного деплоя (typeof-проверка, legacy-режим с однократным console.error); Flowmeter.gs/WorkSchedule.gs — _requireRead/_requireEdit из матрицы (прежние списки ролей — legacy-запас); (Task 296/Этап 4) index.html — KipAuth._serverAccess + _fetchMyAccess (вход/загрузка/heartbeat: правка галочки доезжает до открытой вкладки ≤ ~10 мин) + _applyServerAccess — ОВЕРЛЕЙ ROLE_ACCESS от нетронутого снимка _accessBase (права → группы страниц; found=false → матричные разделы снимаются — fail-closed, зеркалит сервер; карта недоступна → легаси-карта, сервер всё равно гейтит; кэш kip8_my_access в localStorage валиден только для той же роли), FlowmeterData._computeCanInputReadings — flowmeter.input из матрицы (легаси-список «дежурный+Админ» — запас). ИЗМЕНЕНИЯ ПОВЕДЕНИЯ (осознанные, по матрице): «ИТР8 pro» — ПРОСМОТР графика работы; «ИТР ИОС» — ВВОД показаний расходомеров (фикс репорта пользователя: сервер по матрице пускал, клиент строил кнопку по захардкоженному списку — кнопки не существовало). ПЕРЕНЕСЕНО: index.html (патч 12 хунков 372530a..3bf42a4, все с offset -15 — изоляция не затронута; верификация: diff(kip8test@3bf42a4, kip8-новый) == базовый изоляционный дифф Task 292 (82 строки), т.е. байт-в-байт), sw.js v409→v410 (+комментарий партии), scripts/{RoleMatrixInit,RoleMatrix,RoleMatrixGate,Flowmeter,WorkSchedule}.gs (копии байт-в-байт — Flowmeter/WorkSchedule на базовой линии Task 292 были идентичны), scripts/Code.gs (3 хунка вручную: док-строки kip8 сохранены — Task 199/200/222, комментарий WEB_APP_URL, позиция case updatePeriodReading), SW-ассерты kipia-v409→v410 в 6 тест-файлах + подписи тестов v409→v410. Тесты: 1324 passed / 0 failed (паритет с kip8test@3bf42a4). Верификация: node --check ×6 (.gs через временные .js — Node 24 не принимает .gs напрямую) + sw.js; browser-check task296k 20/20 (ИТР8 pro — «График работы» виден, кнопок ввода нет; ИТР ИОС — кнопка «Ввести показания» в карточке, кэш kip8_my_access записан; гость — getMyAccess не запрашивается; 0 JS-ошибок; localStorage БЕЗ префикса kip8test:); VLM ×1 — вёрстка без дефектов. ДЕПЛОЙ ПОЛЬЗОВАТЕЛЯ: в Apps Script НИЧЕГО (kip8 и kip8test — ОДИН бэкенд AKfycbyt…, серверная часть матрицы уже задеплоена в Этапе 3: RoleMatrixGate + RoleMatrix + Code + Flowmeter + WorkSchedule + New version); фронтенд — GitHub Pages подхватит сам (1–3 мин после push), затем Ctrl+Shift+R ×2 (SW v409→v410); см. scripts/DEPLOY-Task296-role-matrix-transfer.md); 2026-09-01 (post-Task 292: перенос партии Tasks 286-292 из kip8test@372530a в боевой kip8 одним инкрементом SW kipia-v409 — по указанию пользователя «переноси все изменения в боевой kip8». МЕТОД: де-изоляция kip8test/index.html (удаление isolateLocalStorage, снятие префиксов kip8test: localStorage, возврат 4 осознанно-разных комментариев Task 242/243/284) — верифицирован байт-в-байт на базовой линии Task 285 (де-изоляция bb44f97 == kip8@431da6e). ПАРТИЯ: (Task 286) ввод показаний «за месяц» для Хозрасчёта №1 — значение = расход за период, запись ТОЛЬКО в hozraschet_archive: колонка R entryType («сутки»/«неделя»/«месяц», self-heal заголовка R1, чтение 18 колонок), meters-строка НЕ меняется («Последние показания» остаются суточными), отдельный маршрут flowmeter.updatePeriodReading (+case в Code.gs — СТАРЫЙ сервер отвечает «Unknown action» и ничего не портит), бейджи «нед»/«мес» + диапазоны дат в хронологии, график только по суточным, getRecentAllMeters пропускает агрегаты; (Task 287→288) блок «Передано показаний (контроль)» УДАЛЁН вместе с логикой (flowCountStats/_buildStatsHtml/CSS .flow-stats-*); (Task 289) форма ввода «за неделю» убрана (chips: сутки/месяц), сервер ПРИНИМАЕТ entryType=«неделя» для legacy-клиентов, дни периода в архиве ВКЛЮЧИТЕЛЬНО (01.08–31.08 = 31 день; FlowmeterArchive .gs); (Task 290) счётчик «За неделю» — ПОСЛЕДНЯЯ ПОЛНАЯ календарная неделя (пн–вс, закрывшаяся в прошлое воскресенье): сумма суточных т И Гкал по последней записи каждой даты, период подписан («За неделю 24.08–30.08.2026: 345,60 т · 1,260 Гкал»), автообновление в начале недели БЕЗ записи в архив, только №1 (dailyMode); (Task 291) счётчик — ОТДЕЛЬНОЙ строкой ПОД «Хронология показаний», перенос текста строго после двоеточия (подпись/значение nowrap), непрозрачные фоны #372e2a/#463e38 (тёмная) и #f5f0eb/#ebe5de (светлая); (Task 292) заголовок графика dailyMode «Показания (посуточно), т» — уточнение суточных данных, ветка «Расход, <ед.>» не тронута. ПЕРЕНЕСЕНО: index.html (де-изоляция, 495 изменённых строк), sw.js kipia-v408→v409 (+комментарий партии), scripts/Flowmeter.gs и scripts/FlowmeterArchive.gs (копии из kip8test), scripts/Code.gs (точечно вставлен case updatePeriodReading, док-строки kip8 сохранены), tests/test-flow-period-input.js (новый, SW-ассерты адаптированы kipia-v409), tests/extract-functions.js (копия: +flowWeekCounterStats/flowWeekRangeLabel/flowPrevMonthRange/flowDateToInputVal/flowDateToMdy/flowEntryTypeAcc), тест заголовка графика в test-flowmeter-validation.js, run-all.js (+require), SW-ассерты v408→v409 в 5 тест-файлах. Тесты: 1230 → 1324 passed / 0 failed (паритет с kip8test@372530a). Верификация: node --check ×3 (.gs) + извлечённый блок _buildArchiveChart; browser-check Task 292-kip8 — 26/26 PASS (заголовок «Показания (посуточно), т» в DOM, 8 суточных баров, счётчик под заголовком с периодом и суммой 345,6 т + 1,26 Гкал, запись текущей недели исключена, фоны #372e2a/#463e38/#f5f0eb/#ebe5de, блока контроля нет, 2 chips без «За неделю», месяц → updatePeriodReading (entryType=«месяц»), сутки → updateReading, №2 «Расход, м³» без счётчика, 0 JS-ошибок, без скролла); VLM ×2 (тёмная/светлая) — вёрстка без дефектов (OCR мелкого 11px-заголовка «посуточно» иногда читает как «построчно» — артефакт распознавания, DOM-текст точен). ДЕПЛОЙ ПОЛЬЗОВАТЕЛЯ: сервер — 3 файла в Apps Script (Flowmeter.gs + FlowmeterArchive.gs + Code.gs) → «Управление развертываниями → Новая версия» у развёртывания AKfycbyt… (НЕ «Новое развёртывание»!), затем фронтенд — Ctrl+Shift+R ×2 (SW v408→v409); см. scripts/DEPLOY-Task292-flow-batch-transfer.md.); 2026-09-01 (post-Task 285: перенос партии Tasks 274-284 из kip8test@bb44f97 одним инкрементом SW kipia-v408 — МОДУЛЬ ОТПУСКОВ «Графика работы»: лист «Отпуска» таблицы табель_КИП_ИОС (A id / B таб_номер / C часть 1..3 / D-E даты / F комментарий; периоды 2-3 частей на год), серверные workSchedule.listVacations/addVacation/deleteVacation (валидация: часть 1..3, конец ≥ начала, без пересечения, уникальность части, аудит), generateMonth шаг 4.5 — «О» на все календарные дни периода с приоритетом «руч > отпуск > инструктаж > смена» и удалением устаревших авто-«О» (идемпотентность); страница «Отпуска» (форма + сводка по сотрудникам) + субнавигация модуля (Шахматка/Сотрудники/Инструктажи/Отпуска), план «О» пунктиром в пустых ячейках шахматки; VacationsInit.gs — авто-создание листа «Отпуска» (шапка, формат «@» для таб_номера, валидации, подсветка пересечений, идемпотентность, force-режим, Task 281 — ручной ввод таб. номера в B); VacationsDiagnose.gs — read-only диагностика (Task 280); диалог «Сформировать» — весь год ИЛИ только месяц (kipConfirm altText, Task 278); видимые ошибки сервера: KipToast=алиас showToast, err.serverMessage, хелпер _apiErrText (Task 282 — «ничего не происходит» устранён); WEB_APP_URL — развёртывание AKfycbyt…/exec (в kip8 с Task 245; пробами Task 284 доказано: проект полный и код свежий; kip8test синхронизирован с тем же URL — ОДИН бэкенд на оба фронтенда); справочные scripts/{Code,WorkSchedule,VacationsInit,VacationsDiagnose}.gs в репо актуальны; тесты 1084 → 1230 passed (+146 = дельта kip8test 1221 → 1230 минус kip8test-only, плюс 5 новых файлов test-vacations-*.js и test-deploy-url.js); post-Task 272: перенос из kip8test@7d69528 одним инкрементом SW kipia-v407 — заявка пользователя из 4 пунктов: 1) кнопка «Календарь» переделана в «ОБНОВИТЬ» — клик открывает диалог kipConfirm «Обновление календаря» с источником «calendar.legalic.ru (федеральный, официальные нормы)», датой обновления (офлайн-кэш старше 7 суток помечается) и вопросом «Обновить производственный календарь на {год} год?»; иконка — круговые стрелки; на время force-запроса кнопка выключена «Обновление…», после — тост «Календарь обновлён»; 2) ШТОРКА НАСТРОЕК УДАЛЕНА — выбор региона (89) и поле токена production-calendar.ru убраны, регион фиксирован 42 (Кемеровская область - Кузбасс), цепочка legalic → isDayOff; 3) «Сформировать» строит шахматку на ВЕСЬ ГОД — 12 последовательных вызовов workSchedule.generateMonth, прогресс «Формируется… N/12», итоговый тост с суммами за год; при сбое месяца — остановка, месяцы сохраняются; 4) кнопки перенесены в ЛЕВОЙ ВЕРХНИЙ угол бара (окно данных — справа, бар 106px); тесты 1072 → 1084 passed; post-Task 270: перенос партии Tasks 269-270 из kip8test@c924c7a одним инкрементом SW kipia-v406 — окно календаря в баре «Графика работы»: нормы ДВУМЯ подстолбиками (счётчики дней слева, часы 40/36/24 + «На год» справа от них), окно шириной ПО ТЕКСТУ в правой части бара / все кнопки единой высоты 34px в левой нижней части, «Загрузка…» шрифтом расходомеров (.flow-loading + .flow-loading-text 16px/600); высота окна ПОД ПЯТЬ СТРОК (95px), светлая тема — фон окна темнее (#e9e7de), заголовки столбцов — цветные ПЛАШКИ (синяя «Норма» / красная «Праздники и переносы») с ярким текстом, бар узкий по высоте окна — отступы 5px (106px); post-Task 267: перенос партии Tasks 264-267 из kip8test@76d84e5 одним инкрементом SW kipia-v405 — окошко производственного календаря в баре кнопок графика (нормы 40/36/24 ч + праздники и переносы месяца), звёздочка сокращённых предпраздничных дней в шапке шахматки, День города Кемерово 12 июня вместе с Днём России (регион 42); «Сформировать» — только через диалог подтверждения «Формирование шахматки» (усиление kipConfirm: заголовок/надписи кнопок title/okText/cancelText, Escape/Enter, защита от двойного срабатывания); ревизия окошка — столбики «Норма» слева / «Праздники и переносы» справа, окошко в левой части бара / все кнопки в правой, статическая высота бара (160px десктоп / 132px мобильный) + скролл внутри окошка, убраны строка «источник: …, обновлено …» (осталась в шторке) и бейдж «официальные нормы» (официальность — тултип столбика норм); анимация бегущих точек «Загрузка…» шахматки (классы расходомеров .flow-loading-dots), Документация ИОС — обе кнопки в одном .menu-btn-row с авто-размещением сеткой как в Библиотеке, закрепление «Графика работы» на главной свайпом (SUBSECTIONS), полная цепочка крошек «Главная / Документация / Документация ИОС / График работы»; post-Task 262: перенос партии Tasks 259-262 из kip8test@aad96f4 одним инкрементом SW kipia-v404 — бордюрчик с эффектом выступа, группировка/сортировка сотрудников (сменные 1-5 → дневные по алфавиту, разделитель групп); производственный календарь РФ: основной источник calendar.legalic.ru (официальные нормы 40/36/24 ч, цепочки переносов в тултипах, версии календаря, 2027 preliminary), резерв isDayOff GitHub, опция production-calendar.ru с токеном, офлайн Сб/Вс + ст. 112; День шахтёра Кузбасса (последнее воскресенье августа, Закон КО № 186-ОЗ) — оверлей поверх любого источника; post-Task 258: перенос партии Tasks 254-258 из kip8test@d695de1 одним инкрементом SW kipia-v403 — визуальная разметка шахматки: розовый фон пустых ячеек выходных (Сб/Вс), тонкие красные границы выходных/рабочих дней вне шапки, должность с режимом занятости из «Сотрудников» табель_КИП_ИОС («Слесарь КИПиА смена №1» / «дневной»), непрозрачный тулбар и прямые углы кнопок/селектов, авто-подгонка под размер окна (целые высоты строк, ярусы уплотнения, ResizeObserver), завершающая полоса-бордюрчик 5px внизу таблицы, скрытие полос прокрутки; post-Task 253: перенос Tasks 249-252 из kip8test@4a6da97 — крошки «Графика работы» с человекочитаемыми метками; десктопная шахматка без легенды, растянута до низа окна, тулбар между крошками и графиком, коды статусов в ячейках, непрозрачный фон, автоподгонка ширины; попап ввода статусов у ячейки с локальным накоплением правок и пакетным сохранением кнопкой «Сохранить»; ранее Task 248 — столбец «Комментарий» хронологии в 4 строках бинарным поиском; Task 247 — дата «за ДД.ММ.ГГГГ г.» неразрывным блоком; Task 245 — синхронизация Tasks 192-238 + Task 241 финал: модуль WorkSchedule; Tasks 242-244 — kip8-специфичные фиксы)
> **Текущая версия кэша:** `kipia-v411`
> **Текущая версия десктопа:** `2.1.7` (kip8-desktop)
> **Базовый репозиторий:** https://github.com/bloknett-design/kip8
> **Живая страница:** https://bloknett-design.github.io/kip8/
> **Матрица доступа KIP8_Access (13 прав × 12 ролей):** https://docs.google.com/spreadsheets/d/1TmmNZLUArWH38F6NX0gMGar8LMNMQomm_FaGZv9osyk/edit
> **Десктопный репозиторий:** https://github.com/bloknett-design/kip8-desktop

---

## ⚠️ ВАЖНО: это боевой репозиторий

Этот промт описывает **боевой** репозиторий `kip8`, который обслуживает пользователей приложения «КИПиА». Изменения сюда переносятся вручную из тестового репо `kip8test` после проверки (см. раздел «Правила работы → Перенос в kip8»). Прямой коммит в `kip8` без проверки в `kip8test` — крайне не рекомендуется.

**Критические отличия от тестового репозитория kip8test:**
- Все пути в `manifest.json` — `/kip8/` (не `/kip8test/`)
- Имена кэшей в `sw.js` — `kipia-v*`, `kipia-images-v3` (не `kipia-test-v*`)
- В `index.html` **НЕТ** обёртки `isolateLocalStorage()` — префикс `kip8test:` не используется, ключи `localStorage` без префикса
- Workflow `schedule:` включён для части синхронизаций (cron ежедневно); kip8 работает на 2 ч раньше kip8test, чтобы не дублировать запросы к Google Sheets
- **В этом репозитории СОБИРАЕТСЯ APK** — TWA-манифест настроен для основного репо `kip8` (Bubblewrap CLI, package `io.github.bloknett_design.twa`)
- **GitHub Action `sync-to-desktop.yml`**: при пуше в `index.html` автоматически синхронизирует изменения в репозиторий `kip8-desktop` (через секрет `DESKTOP_SYNC_TOKEN`)

> ⚠️ **Примечание про `isolateLocalStorage`**: в этом документе могут встречаться упоминания функции `isolateLocalStorage()` и префикса `kip8test:`. Они описывают соответствующий механизм **тестового** репо `kip8test` (для контекста переноса). В `kip8` этой функции **НЕТ** — `index.html` kip8 не содержит блока `isolateLocalStorage()` и префикс `kip8test:` не используется. При переносе из kip8test в kip8 этот блок удаляется (см. `scripts/prepare-kip8-transfer.py`).


---

## 🗺️ Связь между 4 репозиториями

```
                            ┌─────────────────────────────────────┐
                            │        ИСТОЧНИК ПРАВОК              │
                            │   kip8test  →  kip8                  │
                            │   (test PWA)   (боевой PWA, APK)     │
                            └────────────┬────────────────────────┘
                                         │ GitHub Action
                                         │ sync-to-desktop.yml
                                         │ (по пуше в index.html)
                                         ↓
                            ┌─────────────────────────────────────┐
                            │        ДЕСКТОП-ЗЕРКАЛА              │
                            │   kip8test-desktop  ←  kip8test     │
                            │   kip8-desktop       ←  kip8        │
                            │   (Electron, своя нумерация v2.1.x) │
                            └─────────────────────────────────────┘
```

| Репозиторий | Тип | Кэш PWA | Версия десктопа | Назначение |
|-------------|-----|---------|------------------|------------|
| `kip8test` | PWA | `kipia-test-v539` | — | Активная разработка, проверка перед боем |
| `kip8` | PWA + APK | `kipia-v411` | — | Боевой сайт + сборка APK (TWA через Bubblewrap) |
| `kip8test-desktop` | Electron | — | `2.1.7` | Тестовый десктоп (зеркало kip8test) |
| `kip8-desktop` | Electron | — | `2.1.7` | Боевой десктоп (зеркало kip8) |

**Поток изменений:**

1. Правки делаются в `kip8test` (PWA) или `kip8test-desktop` (Electron-специфичное)
2. После проверки переносятся в `kip8` (PWA) или `kip8-desktop` (Electron)
3. При пуше в `index.html` `kip8test` → автосинхронизация в `kip8test-desktop` через `sync-to-desktop.yml` (секрет `DESKTOP_SYNC_TOKEN`)
4. При пуше в `index.html` `kip8` → автосинхронизация в `kip8-desktop` тем же workflow
5. Кэш-версия в `sw.js` инкрементируется при любом изменении файлов приложения

**Ключевые отличия kip8 vs kip8test:**

- Пути в `manifest.json`: `/kip8/` vs `/kip8test/`
- Имена кэшей в `sw.js`: `kipia-v*` vs `kipia-test-v*`, `kipia-images-v3` vs `kipia-images-test-v3`
- `index.html`: в kip8test есть обёртка `isolateLocalStorage()` (префикс `kip8test:` для localStorage-ключей, кроме сессионных `kip8_*`); в kip8 этой обёртки **НЕТ**
- Расписание cron: kip8 на 2 ч раньше kip8test (чтобы не дублировать запросы к Google Sheets)
- APK собирается только в `kip8` (TWA-манифест `io.github.bloknett_design.twa`)

**Ключевые отличия десктоп-репозиториев от PWA-репозиториев:**

- `index.html` — точная копия родительского PWA (синхронизируется автоматически, **НЕ редактировать вручную**)
- **НЕТ** `manifest.json`, `sw.js`, `.nojekyll`, `.well-known/` — PWA-файлы не нужны в Electron
- **НЕТ** `scripts/` — синхронизация данных идёт в PWA-репозиториях
- **СВОИ**: `electron/main.js` (точка входа Electron с `autoUpdater` + `cleanCacheOnStartup()`), `package.json` (appId, productName, version), `charts-desktop.js` (Task 146, только Electron), `devices-table-desktop.js` (Task 148, только Electron)
- Деплой: через тег `v*` (сборка + GitHub Releases через `build-desktop.yml`); обновление — через `electron-updater` `autoUpdater`
- Версия `2.1.7` (отдельная нумерация от PWA)


---

## Контекст

Ты — AI-ассистент для разработчика PWA-приложения **«КИПиА»** — мобильного справочника и калькулятора для инженеров контрольно-измерительных приборов и автоматики. Приложение работает офлайн (Service Worker + NetworkFirst для данных), устанавливается на телефон как нативное (TWA через Bubblewrap — только в боевом `kip8`), распространяется напрямую APK-файлом, а также собирается как Electron-десктоп (`kip8-desktop` / `kip8test-desktop`).

Приложение включает: систему доступа через Email+OTP с матрицей прав KIP8_Access (Apps Script + Google Sheets), 12 ролей пользователей, гостевой режим, инженерные калькуляторы (конвертер единиц, шкала-сигнал, расчёт погрешностей по ГОСТ 8.401-80, буйковые уровнемеры, датчики температуры, сужающие устройства), библиотеку документов, раздел **КИП ИОС** (приборы/блокировки/клапаны/регуляторы/проекты/кабельный журнал/план корпуса 114), раздел «Расходомеры хозрасчётные» (двусторонняя синхронизация с Google Sheets), секретные разделы (сапёр, телефонный справочник), админ-панель.

**Где ведётся активная разработка:** в `kip8test` (тестовый PWA) и `kip8test-desktop` (тестовый десктоп). В `kip8` (боевой PWA) и `kip8-desktop` (боевой десктоп) изменения попадают только после проверки в тестовом репозитории.


---

## Источники проекта

| Что | Где |
|------|------|
| Тестовый PWA (GitHub) | https://github.com/bloknett-design/kip8test |
| Боевой PWA (GitHub) | https://github.com/bloknett-design/kip8 |
| Тестовый десктоп (GitHub) | https://github.com/bloknett-design/kip8test-desktop |
| Боевой десктоп (GitHub) | https://github.com/bloknett-design/kip8-desktop |
| Живая страница тестового PWA | https://bloknett-design.github.io/kip8test/ |
| Живая страница боевого PWA | https://bloknett-design.github.io/kip8/ |
| Apps Script Web App — развёртывание пользователя AKfycbyt… (auth + admin + cable journal + flowmeter + workSchedule + матрица прав; ОДИН бэкенд на kip8 и kip8test) | `https://script.google.com/macros/s/AKfycbyt2sjbJ8xT5UPKDlYj4q-CV-5pH_Yrv5COrg0PIpp92snpQULUNtJC__pMnQ0h6feNlA/exec` |
| Матрица доступа KIP8_Access (права, Task 293-296) | https://docs.google.com/spreadsheets/d/1TmmNZLUArWH38F6NX0gMGar8LMNMQomm_FaGZv9osyk/edit |

### Источники данных (Google Sheets, публичные ссылки)

| Раздел | Лист | Google Sheets ID |
|---------|------|-------------------|
| Приборы КИП ИОС | «Приборы_app» (в «Перечень КИП ИОС рабочий.xlsx») | `1eUUwwulUvKUGWTgQ__XP-y7z1aEkt5Wy` |
| Блокировки | «Блокировки_app» | `1eUUwwulUvKUGWTgQ__XP-y7z1aEkt5Wy` |
| Клапана | «Клапана_app» | `1eUUwwulUvKUGWTgQ__XP-y7z1aEkt5Wy` |
| Регуляторы | «Регуляторы_app» | `1eUUwwulUvKUGWTgQ__XP-y7z1aEkt5Wy` |
| Проекты | «Проекты» (в «Проекты_app.xlsx») | `1IQq8S4-Qao1eJKli3zgMvTpA2exkGYg0` |
| Кабельный журнал | через Apps Script (`CableJournal.gs`) | приватная таблица (доступ через Apps Script) |
| Расходомеры хозрасчётные | лист «hozraschet_meters» через Apps Script (`Flowmeter.gs`) | `1enZSq7K8pwJVzaAI_tbXZtvATqARTxH0lSU4c-wc1eY` |
| Экзаменационные билеты | «Экзаменационные билеты_app.xlsx» | `1D8ElnUF3_ucNCpl0kF3PcVGltF08uoJK` |
| Телефонный справочник | статический коммит (без sync-скрипта) | — |
| Пользователи и сессии | через Apps Script (`Utils.gs` → Admin-объект) | приватная таблица **KIP8_Access** (листы users/sessions/audit_log) |
| **МАТРИЦА ПРАВ** (Task 293-296) | листы matrix/permissions/roles — галочки = права (13 прав × 12 ролей) | `1TmmNZLUArWH38F6NX0gMGar8LMNMQomm_FaGZv9osyk` |

### Apps Script — серверная часть (НЕ в репозитории)

Код Apps Script (`Code.gs`, `Utils.gs`, `CableJournal.gs`, `Flowmeter.gs`, `FlowmeterArchive.gs`, `WorkSchedule.gs`, `ValidationRules.gs`, `VacationsInit.gs`/`VacationsDiagnose.gs`, `RoleMatrix.gs`, `RoleMatrixGate.gs`, `RoleMatrixInit.gs` — одноразовый init матрицы) **физически не находится** в репозиториях. Он редактируется напрямую в Google Apps Script Web App (script.google.com). В репозиториях хранится только URL `WEB_APP_URL` (в `index.html`, ~строка 28 176), а серверные `.gs` файлы — в `scripts/` как справочные копии.

**Серверные эндпоинты (общий список):**

| Действие | Описание |
|----------|----------|
| `sendOTP` | Отправка 6-значного OTP-кода на email. Создаёт pending-запись с TTL ~10 мин. |
| `verifyOTP` | Проверка OTP → при успехе создаёт сессию (token, userId, expiresAt). |
| `getCurrentUser` | По token возвращает `{userId, email, role}` — для bootstrap и heartbeat. |
| `heartbeat` | Продление сессии (сброс `expiresAt`). Вызывается каждые 5 минут. |
| `logout` | Инвалидация token. |
| `getMyAccess` | Карта прав текущего пользователя по матрице KIP8_Access (Task 295, rmGetMyAccess; `no_session` по конвенции doPost). |
| `cableJournal.list` / `.getColumns` / `.getFilters` / `.appendRow` | CRUD кабельного журнала. |
| `adminListUsers` / `adminCreateUser` / `adminUpdateRole` / `adminResetLogin` / `adminListSessions` / `adminListLogs` | Админ-функции (гейт `admin.panel` — только Админ). |
| `flowmeter.list` / `.updateReading` / `.updatePeriodReading` / `.archive` / `.setComment` / `.getValidationRules` / `.getRecentAllMeters` | Просмотр / ввод показаний (за сутки и за период — Task 286) / архив / комментарии / правила валидации расходомеров. |
| `workSchedule.*` | График работы: шахматка / сотрудники / инструктажи / отпуска (`listEntries`/`listEmployees`/`listTrainings`/`listVacations`/`generateMonth`/`addTraining`/`addVacation` / … — Tasks 201-306; гейт `workschedule.view`/`workschedule.edit`). |


---

## Технологический стек

| Компонент | Технология |
|-----------|-----------|
| Frontend | Vanilla JavaScript (ES6+), без фреймворков; весь код в одном `index.html` (~39.8 тыс. строк, ~2.4 MB) |
| Стили | CSS3 в `<style>` внутри `index.html`, CSS-переменные для тем (dark/light) |
| Архитектура | SPA, 93 страницы (`page-*`), шевроны ‹‹ для навигации |
| Офлайн-режим | Service Worker (`sw.js`) — NetworkFirst для `data/*.json` + App Shell + cache-busting |
| Auth | Apps Script Web App (Email+OTP, session token, heartbeat 5 мин) |
| Хранение | `localStorage` — сессионные ключи `kip8_*` без префикса (общие между kip8/kip8test); настройки с префиксом `kip8test:` (только в kip8test) |
| Тесты | Node.js + кастомный фреймворк (`tests/`, 1433 теста, `node tests/run-all.js`) |
| Сборка APK | Bubblewrap CLI + Android SDK + JDK 17 (только в `kip8`) |
| CI | GitHub Actions (1 CI + 7 sync workflows, cron ежедневно) |
| Деплой | GitHub Pages (автоматически при `git push` в `main`); для десктопа — через тег `v*` |
| Синхронизация данных | Python 3.12 + `openpyxl` + `requests`, Google Sheets xlsx-экспорт |
| Backend (auth/admin/cable/flowmeter) | Google Apps Script (JavaScript-диалект, Web App с `doGet`/`doPost`) |
| Десктоп | Electron + electron-builder + electron-updater |


---

## Структура проекта (PWA — kip8 / kip8test)

```
<repo>/
├── index.html                  # Весь HTML + CSS + JS (single-file, ~39.8 тыс. строк, ~2.4 MB)
│                               # 93 страницы (page-*), включая:
│                               # - КИП ИОС: 7 разделов (Приборы/Блокировки/Клапаны/Регуляторы
│                               #   в .kip-ios-block + Проекты/Кабельный журнал/План корпуса 114)
│                               # - Сапёр (секретная кнопка) + Телефонный справочник
│                               # - Модуль KipAuth (Email+OTP + серверная карта прав, ~строка 28 123)
│                               # - Модуль KipCableJournal (редактирование кабелей, ~строка 31 035)
│                               # - Модуль KipFav (избранное, ~строка 29 677) — v2.0.0
│                               # - Модуль FlowmeterData + FlowFav (расходомеры, ~строка 32 885)
│                               # - Админ-панель (4 раздела: users/sessions/logs/stats)
│                               # - Login screen (2 шага: email → OTP)
│                               # - Страница «Что нового» (page-whats-new) — v2.0.0
│                               # - Десктоп: верхний бар + master-detail (#detailPanel) — v2.0.0
│                               # В kip8test: + обёртка isolateLocalStorage() с префиксом 'kip8test:'
├── charts-desktop.js           # Модуль «Графики» — ТОЛЬКО десктоп (Task 146, загружается loader'ом при IS_ELECTRON)
├── devices-table-desktop.js    # Модуль «Таблица приборов» — ТОЛЬКО десктоп (Task 148)
├── manifest.json               # PWA-манифест (пути /kip8/ или /kip8test/, 7 shortcuts, 4 иконки)
├── sw.js                       # Service Worker (CACHE_VERSION + IMAGE_CACHE_VERSION + NetworkFirst для data/*.json)
├── data/
│   ├── exam-tickets.json       # Билеты (348 вопросов в 4 категориях)
│   ├── phonebook.json          # Телефонный справочник (236 записей, 5 организаций)
│   ├── devices.json            # Приборы КИП ИОС (1312 записей, 24 параметра)
│   ├── lockouts.json           # Блокировки (526 записей)
│   ├── valves.json             # Клапаны (320 записей, 24 параметра)
│   ├── regulators.json         # Регуляторы (268 записей)
│   ├── projects.json           # Проекты (263 записи)
│   ├── cables.json             # Кабельный журнал — read-only зеркало (для счётчика на дашборде)
│   └── flowmeters.json         # Расходомеры хозрасчётные (12 позиций, офлайн-fallback)
├── images/                     # PNG-файлы (иконки PWA, логотипы, сплэш, иллюстрации)
├── scripts/
│   ├── sync-devices.py / sync-lockouts.py / sync-valves.py / sync-regulators.py / sync-projects.py
│   ├── sync-cables.py / sync-flowmeters.py / convert-exam-tickets.py
│   └── (справочные копии .gs-файлов Apps Script)
├── tests/                      # 1433 юнит-теста (Node.js, 20 файлов): run-all.js, test-role-access.js, и др.
├── .github/workflows/          # ci.yml + 7 sync-*.yml + sync-to-desktop.yml + deploy-pages.yml
├── .well-known/assetlinks.json # TWA-верификация (только в kip8 — для APK)
├── .nojekyll                   # Отключает Jekyll (чтобы dotfile-папки были доступны на Pages)
├── README.md
├── worklog.md                  # Журнал работы ИИ-ассистентов (shared multi-agent worklog)
└── Системный_промт_для_приложения_КИПиА.md  # этот файл
```


---

## Функциональные модули

### Главная (`page-dashboard`)
- 2 крупных блока-кнопки: «Инженерные калькуляторы» и «Документация»
- **Закреплённые разделы** (v2.0.0) — подразделы, добавленные на главную через long-press или меню «⋯». Поддерживают перетаскивание (долгое зажатие) для смены порядка и свайп для удаления.
- Нижний бар (`dashboardBottomBar`): 2 кнопки. «Калькуляторы» — видна всем; «Документация» — скрывается для гостей.
- 2 секретные кнопки (скрыты по умолчанию): Сапёр и Телефонный справочник. Открытие: 2 тапа по заголовку приложения в течение 1.5 с.

### Инженерные калькуляторы
- **КИП и А** (6): Конвертер единиц (8 страниц), Шкала-сигнал, Расчёт погрешностей (10 страниц, ГОСТ 8.401-80), Буйковый уровнемер, Датчики температуры (ГОСТ 6651-2009 / ГОСТ Р 8.585-2001), Сужающее устройство (4 страницы, ГОСТ 8.586)
- **Электротехника**: Автоматический выключатель (I=P/(√3·U·cosφ) и т. п.)
- **Геометрия** (6 фигур с inline SVG): Круг, Кольцо, Цилиндр вертикальный/горизонтальный, Сфера, Конус

### Библиотека и экзамены
- Библиотека КИП и А: 4 ссылки на облачные папки Google Drive + внутренний раздел «Электробезопасность»
- Экзаменационные билеты: 4 разряда (75+91+90+92 = 348 вопросов). Тестовый режим с подсчётом результата.

### Секретные разделы (открываются 2 тапами по заголовку)
- **Сапёр** (`page-minesweeper`) — поле 9×9, 10 мин
- **Телефонный справочник** (`page-phonebook`) — 3-уровневая иерархия, 236 записей, аккордеон, поиск, избранное, заметки, swipe-навигация

### КИП ИОС (в Документации → КИП ИОС)
- **Карточка `.kip-ios-block`** с 4 кнопками (с цветовой палитрой):
  - Приборы (`#b85a7a` медно-розовый): 1312 записей, 24 параметра. Вкладки: по производствам / по типу / по наименованию (двухуровневая группировка). Картинки — URL Google Drive. Swipe: тап → по производствам, свайп ← → по типу, свайп → → по наименованию.
  - Блокировки (`#b85a7a`): 526 записей
  - Клапана (`#4a8a8c` бирюзовый): 320 записей, 24 параметра, подгруппы по DN внутри группы по производству
  - Регуляторы (`#7e5ab8` аметистовый): 268 записей
- **3 отдельные кнопки** ниже:
  - Проекты (`#b08048` медно-янтарный): 263 проекта, группировка по отделению → году даты утверждения
  - Кабельный журнал (`#8aa070` оливково-зелёный): 3 страницы (`cable-journal-edit/add/view`), данные из Apps Script (`KipCableJournal`), добавление новых записей (редактирование существующих НЕ поддерживается), кэш в localStorage
  - План корпуса 114 (`#7896b0` сине-стальной): заглушка

### Расходомеры хозрасчётные (v388, Task 88-111)
- 12 позиций расходомеров в Google Sheets `hozraschet_meters` (через Apps Script `Flowmeter.gs`)
- Двусторонняя синхронизация: клиент `FlowmeterData` ↔ Apps Script ↔ Google Sheets
- Поля: id, hoz, param, datePrev, dateCurr, prev, curr, unit, temp, Gcal (для пара), period, modRole, modName, modTimestamp
- Архив в `hozraschet_archive` (записывается при каждом вводе/редактировании)
- Редактирование показаний в течение 1 часа после ввода (`_canEdit` = право ввода + тот же email + modTimestamp < 1 часа)
- Избранное расходомеров — отдельный модуль `FlowFav` (полностью изолирован от `KipFav`)
- Нижний бар «Все / Избранные» (как на дашборде), drag-and-drop в режиме «Избранные»

### График работы / WorkSchedule (Tasks 201-306: перенесён в kip8 в Task 241; партии 274-284/285 — отпуска, 292 — расходомеры, 293-296 — матрица прав, 298-306 — коды Т-12/Т-13 + мероприятия + таб_№ текстом)
- Модуль «График работы» (шахматка сменного и дневного персонала ИОС на месяц) — ЧЕТЫРЕ страницы (субнавигация: Шахматка/Сотрудники/Инструктажи/Отпуска):
  - `#page-work-schedule` — сетка-шахматка (сотрудники × дни месяца), тулбар (месяц/год/кнопка «Сформировать»), легенда статусов
  - `#page-work-schedule-employees` — справочник сотрудников (ФИО, таб. №, тип сменного/дневного, шаблон ротации)
  - `#page-work-schedule-trainings` — инструктажи/обучения/проверки знаний (тип, тема, даты, длительность) + НОВЫЕ типы «прогул» (ПР) и «примечание» (*) — Task 306; карточки с плашками типа, красный маркер «⚠ нет в справочнике» для таб_№ не из «Сотрудников» (Task 304); красный маркер «⚠ нет в справочнике».
- Серверный бэкенд `scripts/WorkSchedule.gs` (Apps Script Web App, SPREADSHEET_ID=1MQtW-CWCmjlu-SAeVBllKDP6NRkiOkmW-7xgOjHskWY, 14 endpoints: + listVacations/addVacation/deleteVacation (Task 274-284); generateMonth — шаги 4/4.5/4.6 (мероприятия + отпуска + авто-«ОТ»/«ОТ» + устаревшие строки), _validateStatusCode по «Коды_статусов» (Task 298), таб_№ текстом через _appendRowKeepText + warnings[] (Task 304)). Роутинг workSchedule.* в `scripts/Code.gs`.
- Доступ (Task 295): **по матрице KIP8_Access** — право `workschedule.view` (по текущей матрице: Админ, ИТР8 pro, ИТР ИОС; запись `workschedule.edit` — только Админ). `_WORK_SCHEDULE_PAGES` в `LVL_*` массивах — легаси-запас (`KipAuth._applyServerAccess` пересобирает список по правам матрицы); WorkSchedule.gs `_requireRead/_requireWrite` читают матрицу (`READ_ROLES`/`WRITE_ROLES` — legacy-запас).
- В сайдбаре: `sidebarWorkScheduleBtn` (Task 241 sidebar-move) внутри сворачиваемой группы «Документация ИОС» (`data-group="docs-ios"`, счётчик «2»). `style="display:none"` до первого запуска `_applyRoleToUI` (как `sidebarAdminBtn`) — чтобы не мелькал для не-Админ.
- На странице Документации ИОС: `workScheduleMenuBtn` (menu-btn) в `.kip-ios-block` после «Расходомеры хозрасчётные».
- 3 bottom-sheet-а: `wsCellOverlay` (правка ячейки шахматки — статус/переработка/замещение/комментарий), `wsEmpOverlay` (новый сотрудник), `wsTrOverlay` (новое мероприятие).
- CSS-классы `.ws-*` (тулбар, сетка, легенда, карточки сотрудников/инструктажей, светлая тема) — 279 строк.
- Инициализация в `navigateTo()`: `WorkSchedule.init()` / `initEmployeesPage()` / `initTrainingsPage()` (вызовы через `setTimeout` 30 мс, как `FlowmeterData.init()`).
- Тесты: `tests/test-work-schedule.js` (344 строки, +33 теста). Проверки: парсинг ISO-дат, алгоритм дня цикла, индекс ключа записи, серверные инварианты WorkSchedule.gs (SPREADSHEET_ID, имена листов, READ/WRITE_ROLES, TRAINING_TYPE_TO_STATUS, 11 endpoint-ов в Code.gs), клиентский модуль в index.html (var WorkSchedule, 3 страницы, кнопка меню, navigateTo init, _WORK_SCHEDULE_PAGES, 3 bottom-sheet-а, CSS-классы).

### Версия 2.0.0 / 3.0.0 — настраиваемый дашборд
- Страница «Что нового» (`page-whats-new`) + кнопка «?» в верхнем баре (с красной пульсацией при непрочитанных обновлениях)
- Избранное `KipFav` для всех 4 каталогов (приборы/блокировки/клапаны/регуляторы/проекты), перетаскивание, swipe
- Десктоп: верхний бар `#desktopTopBar` + master-detail `#detailPanel` (клик по карточке открывает её в правой панели; ширина 45%, кнопка ⇄ меняет панели местами)
- Закрепление подразделов на главной (long-press или меню «⋯»), перетаскивание, свайп-удаление

### Паттерн функций для каталогов КИП ИОС

Каждый из 6 каталогов (devices/lockouts/valves/regulators/projects/cables) использует **единый набор функций** (дублирование — известная проблема, рекомендация — вынести в фабрику):

```
<sec>Esc(str) / <sec>Norm(val) / <sec>Mark(str) / <sec>Plural(n, one, few, many) / <sec>FormatDate(iso)
<sec>InitSorted(mode) / <sec>RenderSorted(mode) / <sec>ToggleGroup(key) / <sec>RenderGroup(key)
<sec>OpenDetail(id) / <sec>RenderDetail(item) / <sec>InitEntryButton() / <sec>UpdateEntrySublabel()
```

**Кабельный журнал — исключение**: использует модуль `KipCableJournal` (после рефакторинга Task 67/73).

### Десктоп-специфичные модули (Task 146-148)
- `charts-desktop.js` (KipCharts) — раздел «Графики» со статистикой КИП ИОС. **Только в Electron**, виден только Админу. Загружается loader'ом при `IS_ELECTRON`.
- `devices-table-desktop.js` — кнопка «Таблица» на «Приборах по производствам», Excel-подобный вид. Только в Electron.


### Десктоп: таблица приборов (Tasks 163-176, `devices-table-desktop.js`)

В десктопе на «Приборах по производствам» работает таблица с Excel-подобным видом:

- **Шапка в два ряда** (Task 175): верхний ряд — кнопки одной высоты 28px (Таблица, Сброс, Метки, Экспорт CSV), квадратные 28x28 для иконок; нижний ряд — счётчик «Показано приборов: N из M»
- **Закреплённая колонка «№»** (Task 171): слева всегда видна, остальные колонки прокручиваются
- **Фильтры по колонкам** (Task 168): кнопка ▾ в шапке открывает dropdown со списком значений; множественный выбор, «Выделить всё», бейдж активного фильтра (янтарная плашка)
- **Подложка в покое** (Task 176): кнопки ▾ имеют цветной фон даже без наведения (голубой/синий), чтобы быть заметными
- **Статистика по колонке** (Task 169): рядом с кнопкой ▾ — бейдж «N», при клике — панель со статистикой (бары значений, «Показать все»)
- **Метки строк** (Task 169): слева от строки — квадратный бейдж для заметок (свои метки пользователя)
- **Ширина колонок мышью** (Task 168): перетаскивание границы в шапке меняет ширину; запоминается в localStorage
- **Клавиатура** (Task 168): Tab/Shift+Tab между ячейками, Enter — открыть карточку, Esc — закрыть
- **Виртуальный скролл** (Task 168): рендерятся только видимые строки (1000+ приборов не тормозят)
- **Кнопка «Сброс»** (Task 172): сбрасывает все фильтры, возвращает к полному списку
- **CSV-экспорт** (Task 163): кнопка в шапке выгружает отфильтрованный список в CSV

### Конвертеры единиц — трёхколоночная схема (Tasks 177-178)

На десктопе (≥1024px) все 8 конвертеров единиц (давление/расход/масса/температура/длина/плотность/время/объём) используют единую трёхколоночную разметку через класс `.conv-3col-page`:

- **Колонка 1 (ввод)**: поле ввода, кнопка «Перевести», список результатов
- **Колонка 2 (таблица)**: заглушка «Результаты расчёта» (до первого расчёта) → таблица перевода (после)
- **Колонка 3 (инфо)**: справочная информация, цвета через CSS-переменные `--conv-info-text`/`--conv-info-bold` (читаемость в светлой теме)

Колонки имеют **независимую прокрутку** (overflow-y auto), на странице `skipScroll` отключает прокрутку к таблице при показе результатов (таблица уже видна в средней колонке). На мобильном (<1024px) — вертикальный стек.

### Инженерные калькуляторы — трёхколоночная схема (Task 179)

На десктопе (≥1024px) все 19 инженерных калькуляторов (18 в разделе «КИП и А» + 1 «Автоматический выключатель» в «Электротехника») используют ту же трёхколоночную разметку `.conv-3col-page`:

- **Колонка 1**: формы ввода (`.scale-form`), кнопка «Рассчитать» (`.scale-calc-btn`)
- **Колонка 2**: заглушка → результаты расчёта
- **Колонка 3**: справочная информация (через `conv-info-block` + CSS-переменные)

Калькуляторы: scale-signal, circuit-breaker, orifice-quick/dp/flow/diameter, error-pressure/temp-rtd/temp-tc/flow/level/generic-number/underline/circle/fraction/scale/kit, buoy-calc, temp-sensors.

**Селекторные страницы** (orifice-select, error-select, buoy-select, error-generic) — это меню выбора, а не калькуляторы, поэтому они **НЕ** используют трёхколоночную схему.

### Десктоп: master-detail и поиск (Tasks 161-167)

- **Счётчики расходомеров** (Task 161): в десктопных табах «Все / Избранные» breadcrumb bar добавлены бейджи с количеством (как на мобильном баре)
- **Перетаскиваемая граница панелей** (Task 162): в master-detail на десктопе можно тянуть границу между левой (master) и правой (detail) панелями; клэмп 1/4-3/4 ширины экрана; ширина запоминается
- **Поиск на вложенных страницах КИП ИОС** (Task 164): поиск работает не только на дашборде, но и на страницах групп приборов/блокировок и Избранного
- **Поиск в группах приборов/блокировок** (Task 165): выделяет жёлтым подстроки совпадений в карточках
- **Вертикальная полоса между «Поиск» и «Таблица»** (Task 166): в верхнем баре десктопа разделитель в стиле бара
- **Фикс лупы наезжающей на «Таблица»** (Task 167): при старте приложения лупа больше не перекрывает кнопку «Таблица»

### Десктоп: LED-кнопка КИПиА в верхнем баре (Task 159)

В десктоп-баре рядом с логотипом — фирменная LED-кнопка «КИПиА»:
- 1 тап → переход на главную (`navigateTo('dashboard')`)
- 2 тапа в течение 0.5 с → переход на секретную страницу (см. `SECRET_BUTTON_IDS` в `index.html`)


### Десктоп: строка крошек и разделитель панелей (Tasks 180-189)

- **Поиск у правого края бара крошек** (Task 180): при открытом поиске `#detailBreadcrumbBar.search-open` контент крошек растёт (`flex: 1 1 0%`) — поле поиска и правые кнопки (✕, ⇄, лупа) стоят у ПРАВОГО края бара, текст крошек обрезается многоточием
- **Шеврон в строке крошек на flowmeter-data** (Task 181): `detailBarChevronClick()` — с открытой detail-панелью закрывает её, без панели делегирует в `chevronTap()` (1 тап = назад, 2 тапа = главная); `updateChevronArrows()` обновляет ОБА шеврона (заголовок страницы + бар крошек); `setFlowmeterBreadcrumbContent()` — захардкоженные крошки расходомеров в одном месте; после закрытия панели через ✕ крошки flowmeter-data восстанавливаются автоматически (setTimeout 0 в `closeDetailPanel()`)
- **Три точки на разделителе панелей** (Tasks 182-187): на разделительной линии master-detail — колонка из трёх вертикальных точек (диаметр ~3.8px, шаг 10px), центрирована НА самой 2px линии (`translate(-50%, -50%)`), видна в обычной и swap-раскладке; цвет через CSS-переменную `--dpr-dot` (отдельно для тёмной/светлой темы), pill-фон и mask-градиент из Task 182-185 убраны
- **Мобильная версия — «Избранное»** (Tasks 188-189): в шапке «Избранного» на мобильном (<1024px) лупа и поле поиска скрыты, заголовку возвращён стандартный `padding-right: 16px` (ID-специфичность перебивает `:has()`-правила Task 164) — заголовок/счётчик/«Удалить все» выровнены на одной линии; на десктопе поиск в «Избранном» сохранён без изменений
- **Секретные кнопки на главной** (Task 189): «Сапёр» и «Телефонный справочник» выровнены по остальным кнопкам главной — на мобильном полноширинные в одну колонку (наследуют `#page-dashboard .menu-btn-row`), на десктопе сетка 3-в-ряд (`repeat(3, 1fr)`, gap 20px) с габаритами закреплённых плиток (label 17px/600, sublabel 13px, radius 14px, `var(--card-bg)`); у «Сапёра» подзаголовок «Три уровня сложности»; инлайн-цвета подписей (золото/синий) сохранены как «секретная» идентификация
- **«График работы» в группе «Документация ИОС» + зебра расходомеров контрастней** (Task 240-241, ЧАСТИЧНЫЙ перенос из kip8test@96039d0): применена ТОЛЬКО зебра — в светлой теме список карточек расходомеров (`page-flowmeter-data`, `.flow-card`): odd-ряд потемнее (`rgba(243,233,223,0.96)` vs было `248,242,238`), even без изменений. Разница R-канала 4 → 9 — заметно виднее, но не «полосато». Sidebar-move часть (`sidebar-item` «График работы» внутри группы `data-group="docs-ios"`) — применена позже в Task 241 финал (см. ниже). SW: kipia-v393 → v394. Перенос в kip8-desktop — автоматически через GitHub Action `sync-to-desktop.yml` при пуше в `kip8/index.html`.
- **ФИНАЛЬНЫЙ перенос Task 241 + синхронизация Tasks 192-238** (Task 241 финал + Task 245): модуль WorkSchedule (Tasks 201-239) + Task 241 sidebar-move перенесены из kip8test@96039d0/555eb20 в kip8 — JS-модуль `var WorkSchedule` (+770 строк), 3 HTML-страницы, 3 bottom-sheet-а, 279 строк CSS `.ws-*`, 11 `workSchedule.*` роутов в Code.gs, 854 строки WorkSchedule.gs (сервер). Плюс UI-улучшения расходомеров (Tasks 192-238: единая строка «Последние показания» с датой, sub-значения temp/gcal, валидация показаний Task 199/222/235-236, комментарии Task 195/232/237, состояния загрузки Task 238), Сапёр-достижения (Tasks 191-194), серверная валидация ValidationRules.gs. Сохранены kip8-специфичные фиксы Tasks 242-244 (updateViaCache:'none', !important на #sidebar.active, wsTrSheet-фикс). SW: kipia-v394 → v399 (через Tasks 242-245). Тесты: 509 → 809 passed.
- **Дата «за ДД.ММ.ГГГГ г.» — неразрывный блок** (Task 247, перенос из kip8test@ed7f623): в детальной карточке расходомера (`.flow-detail-date-inline`) и в карточках списка (`.flow-summary-date-inline`) строка «Последние показания» с датой — при нехватке ширины на мобильных ВЕСЬ блок «за 29.08.2026 г.» переносится на новую строку целиком, а не по отдельным словам. Механика: `white-space: nowrap` на span даты + пробел-разделитель вынесен ЗА пределы span (единственная точка переноса — между названием и датой; пробел внутри nowrap-блока сделал бы неразрывной всю строку и вызвал бы переполнение). SW: kipia-v399 → v400. Тесты: +8/−2 → 815 passed.
- **Столбец «Комментарий» хронологии — весь текст в 4 строках** (Task 248, перенос из kip8test@bfa5f04): в таблице «Хронология показаний» детальной карточки расходомера текст столбцов «Комментарий» и «⚠ Замечания» размещается максимум в 4 строки; если при текущей ширине весь текст не помещается — ширина столбца подбирается так (не меняя 4 строк), чтобы помещался ВЕСЬ текст, без обрезки. Причина бага: эвристика Task 221 `ceil(naturalWidth / 4) + 10` делила текст на 4 равные части, а реальный перенос — по границам слов (комментарий «В показаниях значения за период двух суток 28-29.08.2026» → 5 строк, 5-я срезалась `max-height: 5.2em` + `overflow: hidden`). Фикс: `_applyOptimalWidth` переписан — бинарный поиск минимальной ширины, при которой РЕАЛЬНАЯ высота ячейки (`scrollHeight` при `white-space: normal`, helper `_measureArchiveCellHeight`) ≤ 4 × lineHeight (`getComputedStyle`); ячейки обходятся по убыванию naturalWidth, уже помещающиеся — пропускаются; пакетное измерение naturalWidth (1 reflow); guard при скрытом контейнере (измерения 0 → CSS-fallback); результат применяется как min-width + max-width (механика Task 221). CSS-отсечка 5.2em сохранена как safety cap. SW: kipia-v400 → v401. Тесты: +12/−2 → 825 passed.
- **Перенос Tasks 249-252: «График работы» — крошки, десктопная шахматка, попап ввода статусов** (Task 253, перенос из kip8test@4a6da97, партия 4 задач одним инкрементом SW):
  - **Task 249 — крошки**: у страниц `work-schedule` / `work-schedule-employees` / `work-schedule-trainings` появились записи в `PAGE_PARENTS` (иерархия: dashboard → work-schedule → подразделы) и `PAGE_LABELS` («График работы», «Сотрудники», «Инструктажи и обучения») — хлебные крошки десктопа (≥1024px) показывают человекочитаемые названия вместо raw id («Главная / График работы» вместо «Главная / work-schedule»).
  - **Task 250 + 252 — десктопная шахматка**: коды статусов в ячейках (цвет + код, tooltip с названием); непрозрачный фон ячеек (inline у статусных, `var(--bg-primary)` у пустых — строки не просвечивают); автоподгонка ширины (≥1024px: `table-layout: fixed`, ширина 100%, столбец ФИО 200px с эллипсисом, без горизонтальной прокрутки); легенда под шахматкой удалена целиком (HTML + `_renderLegend` + CSS); шахматка растянута до самого низа окна (`#page-work-schedule` — flex-колонка на всю высоту, `.ws-grid-wrap` flex:1 со своим скроллом и sticky-шапкой, строки делят высоту); тулбар с кнопками — во всю ширину между баром крошек и графиком. Мобильная версия не затронута.
  - **Task 251 — попап ввода статусов**: клик по ячейке открывает попап-табличку «код — название» рядом с ячейкой (`#wsCellPopup`, fixed-позиционирование с клампами, закрытие Esc/кликом мимо); выбор статуса отображается в ячейке ЛОКАЛЬНО (буфер `_PENDING`, пунктирная рамка `ws-pending`), мгновенной отправки на сервер НЕТ; кнопка «Сохранить (N)» — пакетная отправка (последовательные setManualEntry/deleteEntry + синхронизация loadGrid; ошибки остаются в буфере); `beforeunload`-предупреждение при несохранённых правках. Серверная часть не менялась (существующие эндпоинты).
  - SW: kipia-v401 → v402 (одна партия = один инкремент, как в Task 245). Тесты: 825 → 872 passed (+47/−2, включая исторические заметки о kip8test-only версиях v508-v510).
- **Перенос Tasks 254-258: визуальная разметка шахматки, авто-подгонка, бордюрчик 5px** (Task 258, перенос из kip8test@d695de1, партия 5 задач одним инкрементом SW — исходники Tasks 254/255/256/257/258 в промте kip8test):
  - **Task 254 + 255 — разметка**: пустые ячейки выходных (Сб/Вс) — слабый пастельно-розовый фон (#6e4250 тёмная / #f7d9e3 светлая); красные линии-границы между колонками выходных и рабочих дней — тонкие (1px #cc6e73) и приглушённые, только в tbody (из шапки убраны), красятся ОБЕ стороны стыка (Пт ws-boundary-after + Сб ws-boundary-left и т.д. — при border-collapse цвет общей грани может выбрать сосед); под ФИО — строка должности с режимом занятости из таблицы «Сотрудники» файла табель_КИП_ИОС (столбец C — тип, D — смена; хелпер `_posLabel`: «Слесарь КИПиА смена №1» / «Слесарь КИПиА дневной»); тулбар непрозрачный, кнопки и селекты месяца/года — прямые углы.
  - **Task 256 — авто-подгонка под окно**: вместо height:100% (браузер давал строкам ДРОБНЫЕ высоты — 1px-границы «растворялись» антиалиасингом, пропадали разделители сотрудников) высоты строк задаёт `_fitGrid` ЦЕЛЫМИ пикселями (остаток — первым строкам по +1px, таблица ровно до низа окна), при нехватке высоты — ярусы ws-compact/ws-tight (подбор по замеру природной высоты), пересчёт по ResizeObserver + брейкпоинту 1024px; явные целые line-height шапки и колонки ФИО.
  - **Task 257 + 258 — полоса и скроллбар**: завершающая полоса-бордюрчик `.ws-grid-foot` ПОД последней строкой (5px — Task 258 поднял с 3px по заявке; rgba(74,143,199,0.35) тёмная / rgba(20,20,19,0.15) светлая), высота резервируется в _fitGrid (budget = область − шапка − полоса); полосы прокрутки шахматки СКРЫТЫ (scrollbar-width:none + ::-webkit-scrollbar display:none, прокрутка колесом/свайпом жива); высота области — из реальной геометрии floor(rect.height+0.25) вместо clientHeight (защита от дробного масштаба Windows 125%/150%).
  - SW: kipia-v402 → v403 (одна партия = один инкремент; тестовые v512-v516 в kip8 не существовали). Тесты: 872 → 910 passed (+41/−2: UI-тесты Tasks 254-258 портированы, SW-тест v402 → v403 + защита от тестовых версий v512-v516 в боевом sw.js). Верификация: харнесс из реального kip8/index.html — 1280×800 (тёмная/светлая), 1280×620, 1024×768×15 сотр., 1280×740×20 сотр. — 17/17 проверок в каждой (полоса ровно 5px, целые строки, нет скроллбаров, дробный sweep, регрессы Task 254-256); пиксельная проверка 5 нижних строк = полоса в обеих темах.

- **Перенос Tasks 259-262: бордюрчик с выступом, группировка сотрудников, производственный календарь РФ + День шахтёра** (Task 262, перенос из kip8test@aad96f4, партия 3 задач одним инкрементом SW — исходники Tasks 259/260/262 в промте kip8test):
  - **Task 259 — бордюрчик и группы**: полоса `.ws-grid-foot` непрозрачная с эффектом выступа (bevel: блик сверху #85b7dc/#ffffff, тень снизу #0f1b26/#84929e, сплошной фон #35648f/#b3c2ce); сотрудники сортируются `_sortEmployees` (сменные по сменам 1..5 с алфавитом внутри → дневные по алфавиту → без типа в конце, без мутации входа); разделитель групп 2px (#4a8fc7 тёмная / #6e8ba4 светлая) на первой строке дневной группы (`ws-group-first`, строгая проверка стыка — лишних разделителей нет).
  - **Task 260 + 262 — производственный календарь РФ** (модуль ProdCalendar ~1600 строк): ОСНОВНОЙ источник calendar.legalic.ru (REST без ключей/лимитов, CORS *; export года JSON ~152 КБ: типы дней WORKING/WEEKEND/PUBLIC_HOLIDAY/SHORTENED_WORKING/TRANSFERRED_DAY_OFF/TRANSFERRED_WORKING, transferredFrom/transferredTo, часы+минуты 40/36/24, holidayName); РЕЗЕРВ isDayOff GitHub-зеркало; ОФЛАЙН — Сб/Вс + ст. 112 ТК. Task 272: prodcal-опция с токеном УДАЛЕНА (регион фиксирован 42, цепочка legalic → isDayOff); регион и токен больше не настраиваются. Официальные нормы = суммы минут legalic (2026: 247 дн./1972/1774,4/1181,6 ч); версии календаря (RU-FEDERAL-2026-v1) + 2027 PRELIMINARY (проект Минтруда — бейдж «предварительные данные», чип «*»). Цепочки переносов в тултипах: «Выходной, перенесённый с 03.01.2026», «Рабочий день (перенос): выходной перенесён на 03.11.2025 (сокращённый день)».
  - **День шахтёра Кузбасса** (регион 42): последнее воскресенье августа (2024:25.08/2025:31.08/2026:30.08/2027:29.08) вычисляется автоматически (`_minersDayMmdd` + `_applyRegionalOverlay` — оверлей поверх данных ЛЮБОГО источника, не перетирает work/regional; в офлайн-фолбэке тоже) — праздник ws-feast с названием и ссылкой на Закон КО № 186-ОЗ от 27.12.2007; название в тултипах и окошке тулбара (шторка удалена в Task 272).
  - **Интеграция в шахматку**: выходные/праздники/переносы/сокращённые/рабочие субботы по календарю (`_calDayOff` — Сб/Вс + праздники + перенесённые выходные, без рабочих суббот); границы стыков между соседними рабочим и нерабочим днём ПО КАЛЕНДАРЮ (праздник в будни тоже даёт красную границу); Task 264+: окошко норм в тулбаре (столбики норм/праздников, Task 269-270 — подстолбики и плашки); Task 272: чип-кнопка «Обновить» — диалог источника и подтверждения (kipConfirm), шторки настроек НЕТ — регион фиксирован 42, токен упразднён; источник и дата обновления — в тексте диалога. Task 306: кнопка «Обновить» УДАЛЕНА — производственный календарь тихо обновляется при подтверждённом «Сформировать» (ProdCalendar.refreshNow(true), silent). Кэш v2 `ws_pcal_year2_{год}_{регион}` (TTL 7 сут, офлайн-устаревание, version+norms; старые ключи ws_pcal_year_* вычищаются).
  - SW: kipia-v403 → v404 (одна партия = один инкремент; тестовые v517-v519 в kip8 не существовали). Тесты: 910 → 1003 passed (+96/−5: функциональные тесты Tasks 259/260/262 из kip8test + новый набор test-prod-calendar.js (75 тестов: реальные данные legalic 2025-2027, нормы, День шахтёра, переносы, приоритеты, кэш v2), SW-секции kip8test v513-v519 заменены на «Task 262: SW версия v404» с защитой от тестовых версий v517-v519). Верификация: реальный kip8/index.html в браузере — legalic загружен (кэш ws_pcal_year2_2026_42, версия RU-FEDERAL-2026-v1), 30.08.2026 День шахтёра (regional, ws-feast), 09.01.2026 «Выходной, перенесённый с 03.01.2026», шторка: норма 1972 ч + бейдж «официальные нормы» + строка «30 августа — региональный праздник — День шахтёра», 2027 — бейдж «предварительные данные»; 0 JS-ошибок.

- **Перенос Tasks 264-267: окошко календаря в тулбаре, диалог «Сформировать», столбики норм/праздников, точки «Загрузка…», авто-размещение кнопок, закрепление на главной, полные крошки** (Task 267, перенос из kip8test@76d84e5, партия 4 задач одним инкрементом SW — исходники Tasks 264/265/266/267 в промте kip8test):
  - **Task 264 — окошко календаря в баре кнопок**: тулбар графика вырос вниз — добавлено окошко `#wsCalPanel` (нормы месяца 40/36/24 ч + годовая, праздники и переносы месяца с бейджами, легенда); звёздочка `*` у сокращённых предпраздничных дней в шапке шахматки и чипах (тултип «−1 час», `.ws-short-star` #4ac771/#1d7a37); День города Кемерово 12 июня вместе с Днём России (регион 42, «День России · День города Кемерово», только для Кузбасса — `_isCityDay`, комбинированное название и в офлайн-фолбэке); нормы и праздники месяца ПЕРЕМЕЩЕНЫ из шторки в окошко (в шторке — подсказка о переезде; настройки региона/токена/«Обновить сейчас» остались в шторке).
  - **Task 265 — «Сформировать» только через диалог**: кнопка тулбара и generateMonth всегда открывают kipConfirm с заголовком «Формирование шахматки», кнопкой действия «Сформировать» и текстом «Существующие ручные правки будут сохранены…»; генерация стартует только после подтверждения. kipConfirm усилен: opts.title/okText/cancelText, Escape — отмена, Enter — подтверждение (если фокус не на кнопке диалога), защита от двойного срабатывания (settled), экранирование заголовков/надписей.
  - **Task 266 — ревизия окошка**: два вертикальных столбика — «Норма, {месяц}» СЛЕВА и «Праздники и переносы» СПРАВА (`.ws-cp-col`, flex-shrink: 0, на узких экранах праздники переносятся под нормы); на десктопе (≥1024px) тулбар — строка: окошко в ЛЕВОЙ части бара (order 0, flex 1 1 auto), все кнопки — в ПРАВОЙ (`.ws-toolbar-main` margin-left: auto, nowrap); высота бара СТАТИЧЕСКАЯ (160px десктоп / 132px мобильная база), при нехватке места — полоса прокрутки ВНУТРИ окошка (overflow-y: auto + overscroll-behavior: contain); убраны строка «источник: …, обновлено …» (полная подпись источника — в шторке настроек) и бейдж «официальные нормы» (официальность — тултип столбика норм).
  - **Task 267 — четыре UI-заявки**: (1) «Загрузка…» шахматки — бегущие точки: плейсхолдер `#wsEmpty` и loadGrid() рендерят «Загрузка» + 3 спана `.flow-loading-dots` (переиспользуются классы/`@keyframes flowLoadingDot` расходомеров, задержки 0s/0.2s/0.4s); (2) Документация ИОС — обе кнопки («Расходомеры хозрасчётные», «График работы») в ОДНОМ `.menu-btn-row` с авто-размещением сеткой как в Библиотеке (десктоп 3 колонки + grid-auto-rows: 1fr равная высота, мобильный — колонка); (3) «График работы» можно закрепить на главной свайпом — запись `work-schedule` в реестре SUBSECTIONS (label «График работы», sublabel «Шахматка сменного и дневного персонала», target work-schedule, category docs — золотистый стиль ярлыка), полный механизм pin-листа (обёртка .subsection-cell, pin-sheet, togglePin); (4) крошки «Графика работы» — ПОЛНАЯ цепочка: PAGE_PARENTS work-schedule dashboard → docs-ios («Главная / Документация / Документация ИОС / График работы», у «Сотрудников» — 5 сегментов).
  - SW: kipia-v404 → v405 (одна партия = один инкремент; тестовые v520-v523 в kip8 не существовали). Тесты: 1003 → 1049 passed (+46: describe Tasks 264 (День города ×6, окошко ×5), 265 (kipConfirm ×10 — vm-песочница диалога), 266 (столбики ×8), 267 (точки ×4, docs-ios ×4, закрепление ×4, крошки ×3, SW ×2); тесты Task 249 переписаны под полную цепочку крошек; SW-секция «Task 262: SW v404» обновлена на актуальную v405 + защита от тестовых версий расширена до v517-v523, добавлена «Task 267: SW версия v405»). Верификация: браузерная 29/29 PASS на реальном kip8/index.html (живой legalic: кэш ws_pcal_year2_* БЕЗ тест-префикса; окошко — 2 столбика, нормы слева/праздники справа, окошко слева/кнопки справа, 160px, скролл; июнь 2026 «День России · День города Кемерово» + 1972 ч/год; август 2026 День шахтёра; 2027 «предварительно»; диалог «Формирование шахматки» + «Сформировать»/«Отмена» + Escape; docs-ios один ряд/3 колонки/равная высота; свайп → ярлык на главной; крошки полные + кликабельные dashboard/docs/docs-ios; точки 3 спана flowLoadingDot 1.4s 0s/0.2s/0.4s; 0 JS-ошибок) + VLM ×4 (окошко, диалог, крошки). Скриншоты: scripts/port-264-267-*.png.


- **Перенос Tasks 269-270: ревизия бара кнопок и окошка «Графика работы»** (Task 270, перенос из kip8test@c924c7a, партия 2 задач одним инкрементом SW — исходники Tasks 269/270 в промте kip8test):
  - **Task 269 — пять правок бара**: (1) столбик норм окна календаря разделён на ДВА подстолбика — группа `.ws-cp-group.ws-cp-norms` (заголовок «Норма, {месяц} {год}» сверху + контейнер `.ws-cp-cols` из двух `.ws-cp-col`: счётчики дней Рабочих / Выходных и празд. / Сокращённых* СЛЕВА, нормы часов 40/36/24-час + «На год» СПРАВА от них; на узких экранах подстолбик часов переносится под дни); (2) окно УЖЕ — ширина ПО ТЕКСТУ (width: fit-content + max-width: 100%, не растягивается на свободную часть бара); (3) единая высота всех элементов ряда кнопок 34px (`.ws-month-sel, .ws-year-sel, .ws-cal-chip, .ws-generate-btn, .ws-save-btn` + box-sizing: border-box); (4) на десктопе (≥1024px) кнопки в ЛЕВОЙ НИЖНЕЙ части бара (`.ws-toolbar-main` order 0 + align-self: flex-end + margin-right: auto, nowrap), окно с данными — в ПРАВОЙ (`.ws-cal-panel` order 1, flex 0 1 auto); (5) «Загрузка…» шахматки — шрифт как у «Расходомеров хозрасчётных»: статический `#wsEmpty` и loadGrid() используют связку `.flow-loading` + `.flow-loading-text` (16px / 600 / text-primary) + бегущие точки `.flow-loading-dots` (разметка admin-empty удалена).
  - **Task 270 — пять правок окошка**: (1) светлая тема — фон окна #e9e7de (на тон ТЕМНЕЕ тулбара #f0eee6: углублённый блок данных, не выбеливается); (2) высота окна — 95px ПОД ПЯТЬ СТРОК (плашка-заголовок + 4 строки столбика часов; было 120px), на обоих брейкпоинтах; (3) цветовое разграничение заголовков столбцов ФОНОМ — плашки: «Норма, …» СИНЯЯ rgba(74,143,199,0.22) с ярким текстом #93c1ea, «Праздники и переносы» КРАСНАЯ rgba(255,107,107,0.20) с #ff9c9c (светлая тема: rgba(74,143,199,0.16)/#1d5f96 и rgba(255,107,107,0.15)/#b02c2c — контраст AA); плашка inline-block с padding 1px 7px и radius 2px, обнимает текст (align-self: flex-start, не тянется на ширину столбика); старое серое правило светлой темы удалено; (4) текст названий столбцов ЯРЧЕ приглушённого вторичного; (5) бар узкий по высоте окна — отступы 5px со всех сторон (padding 5px, gap колонки 5px, десктопный зазор 5px; высота бара = окно 95px + 2×5px + рамка = 106px, было 137px).
  - SW: kipia-v405 → v406 (одна партия = один инкремент; тестовые v524/v525 в kip8 не существовали). Тесты: 1049 → 1072 passed (+23: describe Task 269 — подстолбики норм ×3, окно по тексту/высота ×2, кнопки слева внизу/окно справа/34px ×4, шрифт «Загрузка…» ×4, SW ×2; Task 270 — плашки-заголовки ×6, светлая тема/узкий бар ×4; тесты Task 266/267 переписаны под новую структуру: ws-cp-group, 95px, кнопки слева внизу / окно справа, flow-loading-разметка #wsEmpty; SW-секция «Task 267: SW v405» переименована в «Task 269-270: SW версия v406», защита от тестовых версий расширена до v517-v525). Верификация: браузерная 45/45 PASS на реальном kip8/index.html (живой legalic: группа норм с двумя подстолбиками — дни слева, часы справа; окно по ширине текста справа / кнопки 34px слева внизу; бар 106px; плашки синяя/красная с ярким текстом, обнимают текст; июнь 2026 «День России · День города Кемерово» + 1972 ч; август 2026 День шахтёра; бар статичен июнь=июль=август=106px; январь 2027 — каникулы 1-8 янв + Рождество, бейдж «предварительно», скролл внутри; «Загрузка…» .flow-loading-text 16px/600; светлая: окно #e9e7de темнее тулбара #f0eee6, плашки #1d5f96/#b02c2c; мобильная 375px — колонка, окно 95px, кнопки 34px, отступы 5px; 0 JS-ошибок) + VLM ×5 (панель, светлая, загрузка, январь-2027, мобильная). Скриншоты: scripts/port-269-270-*.png.

- **Перенос Task 272: кнопка «Обновить», шторка удалена (регион 42), «Сформировать» — на весь год, кнопки в левом верхнем углу** (Task 273, перенос из kip8test@7d69528, один инкремент SW): заявка пользователя из 4 пунктов. 1) **Кнопка «Обновить»** (бывший чип «Календарь», `#wsCalChip`): текст «Обновить», иконка — круговые стрелки (path+polyline); клик вызывает `ProdCalendar.confirmRefresh()` — диалог kipConfirm «Обновление календаря» с кнопкой действия «Обновить»: «Источник: calendar.legalic.ru (федеральный, официальные нормы). / Обновлено: {дата}[ (офлайн-кэш, старше 7 суток)]. / Обновить производственный календарь на {год} год?» (год — из WorkSchedule через `_currentYM()`); подтверждение → `refreshNow()`: кнопка disabled «Обновление…», `ensureYear(год, force)` → `_syncWorkSchedule()` + тост «Календарь обновлён» / «Не удалось обновить: источник недоступен». 2) **Шторка настроек удалена**: HTML `#wsCalOverlay`/`#wsCalSheet`/`#wsCalSheetBody`, селект 89 регионов, поле API-токена production-calendar.ru и подсказки убраны; из ProdCalendar удалены методы openSheet/closeSheet/_renderSheet/onRegionChange/saveToken/regions/regionName/regionHasRegional/setSettings/_fetchProdCal/_parseProdCal, константы _REGIONS_RAW (~2.6КБ) и _PRODCAL_URL, поля _sy/_sm; мёртвый CSS шторки удалён. 3) **Регион фиксирован**: `getSettings()` всегда `{region: 42}` и вычищает старую запись `ws_pcal_settings_v1`; `_REGION_NAME` «Кемеровская область - Кузбасс»; `_cacheKey` = `ws_pcal_year2_{год}_42`; цепочка legalic → isDayOff (prodcal-ветки удалены); старый кэш с подписью prodcalendar читаем (офлайн не теряется); День шахтёра и День города Кемерово — локальный оверлей 42. 4) **«Сформировать» — на весь год**: `generateMonth` → `generateYear` (диалог «Сформировать шахматку на {год} год (все 12 месяцев)?»); `_doGenerateYear()` последовательно вызывает серверный `workSchedule.generateMonth` для 1..12 (сервер не менялся), прогресс «Формируется… N/12» на кнопке, тост «Год {год}: сформировано N зап., обновлено M»; сбой месяца → остановка, тост «Ошибка (месяц M, готово K/12)», сетка перезагружается. 5) **Кнопки — в левом верхнем углу**: ≥1024px `.ws-toolbar-main` `align-self: flex-end` → `flex-start` (Task 269 ставил вниз); окно данных справа, бар 106px; мобильная колонка без изменений. **Task 306: кнопка «Обновить» из пункта 1 УДАЛЕНА** (wsCalChip + confirmRefresh + стили .ws-cal-chip) — производственный календарь теперь тихо обновляется при подтверждённом «Сформировать» (`ProdCalendar.refreshNow(true)`, silent — см. партию Tasks 298-306).
  - SW: kipia-v406 → v407 (один инкремент; тестовой v526 в kip8 не существовало). Тесты: 1072 → 1084 passed (+12 = дельта kip8test 1078 → 1090: −10 устаревших prodcal/регион-тестов, +22 новых Task 272, минус kip8test-only исторические SW-секции; SW-строки адаптированы под нумерацию kip8: «Task 272: SW версия v407», защита от тестовых версий расширена до v517-v526).
  - Верификация: браузерная 36/36 PASS на РЕАЛЬНОМ kip8/index.html (живой legalic: кэш ws_pcal_year2_2026_42 без префикса; июнь 2026 «День России · День города Кемерово» + нормы 40/36/24; чип «Обновить» с polyline-иконкой, onclick confirmRefresh, шторки/токена/селекта региона в DOM НЕТ, openSheet/saveToken undefined, getSettings() регион 42; кнопки top=бар+5px слева, окно справа, бар 106px/окно 95px; диалог «Обновление календаря» — источник legalic, дата, вопрос 2026, «Обновить»/«Отмена», Escape; force-запрос — чип disabled «Обновление…», тост «Календарь обновлён»; «Сформировать» — ровно 12 последовательных вызовов месяцы 1..12 год 2026, прогресс, тост «Год 2026: сформировано 120 зап., обновлено 24», loadGrid; сбой 5-го месяца — вызовы 1..5, тост «4/12», сетка перегружена; мобильная 375px — колонка, переполнений нет; 0 JS-ошибок; KipToast в index.html не определён — заглушка в тесте) + VLM ×2 (тулбар: кнопка сверху, окно справа, без региона/токена; диалог: заголовок, источник, кнопки). Скриншоты: scripts/port-272-*.png (в my-project/scripts).

- **Перенос партии Tasks 274-284: МОДУЛЬ ОТПУСКОВ «Графика работы»** (Task 285, перенос из kip8test@bb44f97, один инкремент SW; исходники Tasks 274-284 в промте kip8test): автоматическая расстановка отпусков в шахматке. **Данные**: лист «Отпуска» таблицы табель_КИП_ИОС (A id / B таб_номер / C часть 1..3 / D дата_начала / E дата_окончания / F комментарий; у сотрудника 2-3 части отпуска на год). **Сервер** (WorkSchedule.gs): listVacations (периоды, пересекающие год), addVacation (валидация: часть 1..3, конец ≥ начала, без пересечения с существующими, уникальность части; аудит), deleteVacation; роутинг в Code.gs — 3 case; generateMonth шаг 4.5: все календарные дни периода (вкл. Сб/Вс) → «О», приоритет «руч > отпуск > инструктаж > смена», устаревшие авто-«О» удаляются (идемпотентность). **Фронтенд**: страница «Отпуска» (форма: сотрудник/часть/даты/комментарий; сводка по сотрудникам с днями), субнавигация модуля (Шахматка/Сотрудники/Инструктажи/Отпуска), план «О» пунктиром в пустых ячейках шахматки. **VacationsInit.gs**: авто-создание листа «Отпуска» (шапка A–F, формат «@» для таб_номера, валидации, подсветка пересечений COUNTIFS, идемпотентность, force-режим; Task 281 — ручной ввод таб. номера в B не отвергается). **VacationsDiagnose.gs** (Task 280): read-only диагностика «отпуска не формируются». **Task 278**: диалог «Сформировать» спрашивает «на весь год или только месяц» (kipConfirm расширен altText — третья кнопка). **Task 282**: фантомный KipToast устранён (алиас showToast, ~105 вызовов включились), KipAuth.api сохраняет data.message в err.serverMessage, хелпер _apiErrText в отпускных кэтчах — «Ошибка: Период пересекается…» вместо кода overlap. **Task 284 (справочно)**: kip8test переключён на развёртывание AKfycbyt…/exec — то же, на которое kip8 указывает с Task 245; ОДИН бэкенд на оба фронтенда, серверные файлы в репо (scripts/*.gs) — справочные копии для ручной синхронизации с редактором Apps Script.
  - SW: kipia-v407 → v408 (одна партия = один инкремент; тестовые v527-v531 в kip8 не существовали). Тесты: 1084 → 1230 passed (+146: test-vacations-generate/init/feedback/diagnose + test-deploy-url, обновления test-work-schedule/test-prod-calendar; SW-ассерты адаптированы под нумерацию kip8: v408, защита от v404/v405/v406/v407; дубль require(test-prod-calendar) в run-all.js убран).
  - Верификация: browser-check 21/21 PASS на РЕАЛЬНОМ kip8/index.html (scripts/task285-browser-check.py, мок ТОЛЬКО fetch: KipToast определён приложением, страница «Отпуска» строится, addVacation отклонён → тост с serverMessage «Период пересекается…», диалог остаётся открыт; валидация «Выберите сотрудника» в тосте; успех → диалог закрыт, тост «Отпуск добавлен…»; 0 JS-ошибок) + VLM ×2 (тост успеха: субнавигация и карточки; тост ошибки: диалог открыт, текст читабелен). Скриншоты: scripts/task285-*.png (в my-project/scripts).
- **Перенос партии Tasks 298-306: КОДЫ Т-12/Т-13, мероприятия ПР/*, таб_№ текстом, отпуск + плановая смена, одна кнопка «Сформировать»** (Task 306, перенос из kip8test@4fc48bc, партия одним инкрементом SW kipia-v410→v411; исходники Tasks 298-306 в промте kip8test):
  - **Task 298 — коды статусов по Т-12/Т-13 (16)**: Д8/Д7,2 (8/7,2-часовые смены дневного персонала), строчные д/н (работа в выходные/праздники), У (учебный отпуск), ПР (прогул), «.» (плановый выходной), ОТ (отпуск), ОВ (отгул), «*» (примечание); регистрозависимые; сервер: generateMonth пишет «ОТ» вместо «О» (шаг 4.5), НОВЫЙ _validateStatusCode — setManualEntry валидирует код по листу «Коды_статусов» (fail-closed: unknown_статус); клиент: fallback 16 кодов, динамический select шита расширенной правки (_fillStatusSelect из справочника, легаси-value — временная опция «(нет в справочнике)»), план отпуска в пустых ячейках = «ОТ» + тултип. Лист «Коды_статусов» живой таблицы уже содержит 16 кодов (включая ПР #EF5350 и «*» #FFAB91).
  - **Task 299 — scripts/StatusCodesInit.gs** (справочная копия): одноразовый GAS-инструмент замены листа «Коды_статусов» на 16 эталонных кодов + миграция «О»→«ОТ» в «Записи_графика» (statusCodesDeployAll ВЫПОЛНЕН в kip8test — повторно НЕ запускать; updateCycleDaysTemplate2 НЕ запускать — лист «Дни_цикла» уже поправлен пользователем).
  - **Task 302 — цвет Д7,2** синхронизирован с Д8 (#FFF9C4) в fallback index.html и эталоне SC_STATUS_CODES (StatusCodesInit.gs).
  - **Task 303 — слой мероприятий И/ОБ/ПЗ**: приоритет статуса дня «ручная > отпуск > плановая смена > мероприятие» (мероприятие больше НЕ затирает смену — «Д8»→«И» устранён); И/ОБ/ПЗ пишутся только на дни БЕЗ смены (шаг 4); НОВЫЙ шаг 4.6 generateMonth — сверка устаревших строк мероприятий (restore/delete, идемпотентность); бейдж .ws-ev-badge в правом нижнем углу ячейки (цвет справочника, не дублирует основной код, пунктирный ws-ev-pending на пустой ячейке, скрыт на днях отсутствия); попап ячейки — секция «Мероприятия в этот день» + «+ Мероприятие…» с префиллом openTrainingForm(таб, дата).
  - **Task 304 — таб_№ хранится ТЕКСТОМ**: НОВЫЙ хелпер _appendRowKeepText (setNumberFormat «@» ДО записи значения) заменил appendRow во всех 4 путях записи таб_№ — addEmployee (Сотрудники!A), addTraining (Инструктажи!B), addVacation (Отпуска!B), setManualEntry (Записи_графика!B); generateMonth — «@» на диапазон вставки колонки B; НОВОЕ поле warnings[] в ответе generateMonth (мероприятия/отпуска с таб_№ не из справочника → тост «⚠ … не найден в «Сотрудниках» — вероятно, потерян ведущий ноль», макс. 3 + «(и ещё N)»); страница «Инструктажи» — красный маркер «⚠ нет в справочнике» (.ws-tr-warn); scripts/TabNumbersFix.gs — разовая починка испорченных числовых ячеек (выполнен в kip8test).
  - **Task 305 — отпуск + плановая смена бейджем**: дни отпуска — код отпуска (ОТ/У) ОСНОВНЫМ значением ячейки, плановая смена по циклу (Дни_цикла; клиентский _plannedShiftAt = алгоритм шага 3 generateMonth, паритет сервер↔клиент) — бейджем .ws-ev-badge.ws-ev-shift в правом нижнем углу в формате мероприятия (цвет справочника: Д #FFE082, Н #B0BEC5, Д8/Д7,2 #FFF9C4; тултип «по циклу (Дни_цикла): <название>»); «отрисовка, не хранение» — Apps Script и «Сформировать» заново НЕ нужны; работает и ДО генерации (план «ОТ» пунктиром + сплошной бейдж); страховка «чип только при пустом evHtml» (мероприятия на днях отпуска скрыты — тултип).
  - **Task 306 — три правки по заявке**: (1) бейдж плановой смены — ТОЛЬКО у сотрудников типа «сменный» (empIsShift = String(emp[«тип»]).trim()==«сменный» в условии vacMain _renderCell; дневные Д8/Д7,2 и без типа — без бейджа; расчёт _plannedShiftAt НЕ менялся — паритет с сервером сохранён, тесты Task 305 живы); (2) мероприятия + НОВЫЕ «прогул» → код ПР и «примечание» → код «*»: _EVENT_CODES [«И»,«ОБ»,«ПЗ»,«ПР»,«*»], _trainingCodeOf + прогул/примечание, форма «Новое мероприятие» 5 типов, _renderTrainings — единая карта, CSS-плашки .ws-type-ПР #EF5350 / .ws-type-\* #FFAB91 (escape-селектор); сервер TRAINING_TYPE_TO_STATUS + прогул/примечание (addTraining принимает; неизвестный тип → invalid_тип), _isEventStatusCode + ПР/* (генерация пишет на дни БЕЗ смены как И/ОБ/ПЗ, на сменных днях — бейдж; шаг 4.6 снимает устаревшие авто-строки ПР/*; РУЧНЫЕ строки ПР/* не трогаются); ПР остаётся в _ABSENCE_CODES (день-прогул = день отсутствия); (3) КНОПКИ «Обновить»+«Сформировать» ОБЪЕДИНЕНЫ: кнопка «Обновить» (wsCalChip, Task 272) УДАЛЕНА вместе с диалогом ProdCalendar.confirmRefresh и мёртвыми стилями .ws-cal-chip; клик «Сформировать» → прежний диалог («Весь год»/«Текущий месяц») → после подтверждения ПАРАЛЛЕЛЬНО с генерацией ТИХОЕ обновление производственного календаря: НОВЫЙ WorkSchedule._refreshProdCalendarQuiet (try/catch) → ProdCalendar.refreshNow(true) — НОВЫЙ параметр silent (без тостов; сбой не мешает формированию, офлайн-кэш TTL 7 суток); вызовы из _doGenerateMonth/_doGenerateYear; _updateCalChip упрощён до renderPanel; окошко норм #wsCalPanel и фоновая автозагрузка живы.
  - **Сервер WorkSchedule.gs МЕНЯЛСЯ** (Tasks 298/303/304/306) — Apps Script: заменить файл + «Новая версия» у развёртывания AKfycbyt… (ОДИН бэкенд на оба сайта; если уже сделано при деплое kip8test — НЕ повторять). Лист «Коды_статусов» уже содержит 16 кодов — НЕ трогать.
  - Тесты: 1324 → 1433 passed / 0 failed (паритет с kip8test@4fc48bc): НОВЫЕ test-work-events.js (27), test-tab-numbers.js (22), test-vacation-shift.js (19), test-task306.js (19) + обновлённые test-prod-calendar/test-vacations-generate/test-vacations-init/test-vacations-feedback/test-work-schedule/test-deploy-url/test-flow-period-input/run-all. Верификация: browser-check 29/29 (scripts/task306-browser-check.py, порт 8931, мок fetch) + содержательный паритет изоляционного диффа. SW: kipia-v410 → v411 (одна партия = один инкремент; тестовые v540-v545 в kip8 не существовали).


---

## 🔐 Система доступа Email+OTP

### Архитектура

```
[Браузер PWA / Electron]
   ↓ fetch POST (Content-Type: text/plain;charset=utf-8)
   ↓ body: JSON.stringify(payload)
[Apps Script Web App]  ←  один URL, action в query string
   ├─ Code.gs (doGet, doPost — роутинг по action; ГЕЙТ rmRequirePerm до модулей — Task 295)
   ├─ Utils.gs (Admin-объект, общие функции, MailApp.sendEmail)
   ├─ CableJournal.gs (cableJournal-объект)
   ├─ RoleMatrix.gs (чтение матрицы KIP8_Access, кэш TTL 300с — Task 294)
   ├─ RoleMatrixGate.gs (rmRequirePerm / rmGetMyAccess — Task 295)
   └─ Flowmeter.gs / FlowmeterArchive.gs / WorkSchedule.gs
   ↓
[Google Sheets — KIP8_Access: users / sessions / audit_log / matrix / permissions / roles (МАТРИЦА ПРАВ); кабели, расходомеры, график работы]
```

**Почему `Content-Type: text/plain;charset=utf-8` (а НЕ `application/json`):** `application/json` — это "non-simple" Content-Type, который заставляет браузер отправлять **CORS-preflight (OPTIONS-запрос)**. Apps Script **не определяет `doOptions`** → `405 Method Not Allowed` → браузер блокирует POST → «Failed to fetch». `text/plain;charset=utf-8` — "simple" Content-Type, preflight не отправляется, POST идёт сразу. Тело запроса остаётся JSON-строкой, Apps Script парсит через `e.postData.contents` + `JSON.parse`.

> **Task 294-296 — права по матрице KIP8_Access:** сервер гейтит каждый защищённый action (`rmRequirePerm`, отказы — аудит `RM_ACCESS_DENIED`); клиент получает карту прав `getMyAccess` при входе/загрузке/heartbeat и строит видимость разделов по ней (`KipAuth._serverAccess`, кэш `kip8_my_access`).

### Ключевые localStorage-ключи (КРИТИЧНО)

| Ключ | Что хранит | Особенность |
|------|-----------|-------------|
| `kip8_session_token` | Текущий session token | **БЕЗ префикса** — общий между kip8 и kip8test (один вход) |
| `kip8_cached_role` | Кэшированная роль (для быстрого bootstrap) | **БЕЗ префикса** |
| `kip8_cached_email` | Кэшированный email | **БЕЗ префикса** |
| `kip8_cached_user_id` | Кэшированный userId | **БЕЗ префикса** |
| `kip8_cj_cache_v1` | Кэш списка кабельного журнала | **БЕЗ префикса** |
| `kip8_cj_cols_v1` | Кэш колонок кабельного журнала | **БЕЗ префикса** |
| `kip8_flow_cache_v1` | Кэш данных расходомеров | **БЕЗ префикса** |
| `kip8_my_access` | Кэш карты прав с сервера (getMyAccess, Task 296) — быстрый старт без мигания разделов; валиден только для той же роли, сбрасывается при logout | **БЕЗ префикса** |
| `kip8test:favorites` | Избранное KipFav | **С префиксом** (только в kip8test, изолировано от kip8) |
| `kip8test:pinned_items` | Закреплённые разделы дашборда | **С префиксом** |
| `kip8test:theme` | Тема (dark/light) | **С префиксом** |
| `kip8test:whats_new_read_3.0.0` | Флаг прочтения «Что нового» | **С префиксом** |
| `kip8test:phonebook_favorites` / `:phonebook_notes` | Избранное и заметки тел. справочника | **С префиксом** |
| `kip8_flow_fav_v1` / `kip8_flow_fav_order_v1` | Избранное расходомеров (FlowFav) | **БЕЗ префикса** (отдельный модуль, изолирован от KipFav) |

> В kip8 (боевой PWA) **все** ключи идут без префикса (isolateLocalStorage() нет). В kip8test (тестовый) обёртка `isolateLocalStorage()` применяется ко всем ключам, **кроме** перечисленных выше `kip8_*`/`kip8_flow_*` (для совместимости с kip8).

### Флоу входа (2 шага)

1. **Шаг 1** — ввод email (`#authEmail`): валидация regex → `api('sendOTP', {email})` → Apps Script отправляет письмо с 6-значным кодом (MailApp.sendEmail) → переключение на Шаг 2
2. **Шаг 2** — ввод OTP (`#otp1`...`#otp6`): авто-переход фокуса, вставка из буфера (`autocomplete="one-time-code"`) → `api('verifyOTP', {email, code})` → сервер возвращает `{token, role, userId, email}` → клиент сохраняет token + кэш роли → `_applyRoleToUI()` → переход на `dashboard`

### Bootstrap стратегия (КРИТИЧНО — Task 33)

При загрузке страницы `KipAuth.bootstrap()` работает по стратегии:

1. **НЕТ ТОКЕНА** → ГОСТЕВОЙ РЕЖИМ (роль «Общий доступ», без heartbeat). **НЕ показывать экран входа принудительно.**
2. **ЕСТЬ ТОКЕН + ЕСТЬ КЭШ РОЛИ** → БЫСТРЫЙ ПУТЬ: мгновенно показать приложение с кэшированной ролью + запустить heartbeat + в ФОНЕ запросить `getCurrentUser` для проверки. Успех → обновить кэш; `session_expired` → тихий переход в гостевой режим; сетевая ошибка → оставить пользователя в приложении с кэшированной ролью.
3. **ЕСТЬ ТОКЕН, НО НЕТ КЭША** → МЕДЛЕННЫЙ ПУТЬ: ждём ответа сервера. `session_expired` → гостевой режим; сетевая ошибка → экран входа с текстом «Не удалось связаться с сервером...» (токен НЕ удаляем, чтобы пользователь мог обновить страницу).

### Классификация ошибок (КРИТИЧНО)

Все ошибки в `api()` помечаются `_kind`:
- `'NETWORK'` — `TypeError: Failed to fetch`, не-OK HTTP, HTML вместо JSON, таймаут, обрыв
- `'SERVER'` — корректный JSON с `data.ok === false` (например `session_expired`, `no_session`, бизнес-ошибка)

**Правило:** NETWORK-ошибки **НЕ** сбрасывают токен и **НЕ** разлогинивают. SERVER-ошибки `session_expired`/`no_session` → реальный logout → гостевой режим. `api()` делает 2 попытки с задержкой 1 сек **только** для NETWORK-ошибок.

### Что НЕ делать в системе auth

- ❌ Использовать `Content-Type: application/json` — сломает POST из-за CORS-preflight
- ❌ Сбрасывать токен при сетевой ошибке — пользователь потеряет сессию из-за временного сбоя
- ❌ Показывать экран входа принудительно после logout — пользователь переходит в гостевой режим
- ❌ Хранить пароли — система использует только OTP
- ❌ Сохранять OTP-коды на клиенте — код одноразовый, проверяется только на сервере


---

## 👤 Роли и доступ

### Карта ролей (12 ролей)

Источник: https://docs.google.com/spreadsheets/d/1TmmNZLUArWH38F6NX0gMGar8LMNMQomm_FaGZv9osyk/edit

> ⚠️ **Task 293-296: ИСТОЧНИК ИСТИНЫ ПРАВ — МАТРИЦА KIP8_Access** (галочки). Таблица ниже — легаси-карта (`KipAuth.ROLE_ACCESS`, запас при недоступности матрицы; сервер гейтит каждый запрос по матрице в любом случае). Как менять права: галочка в листе matrix → сервер ≤ 5 мин (кэш) или мгновенно `roleMatrixInvalidateCache()` → клиент при входе/загрузке/heartbeat ≤ ~10 мин. Серверные проверщики: `rmRequirePerm` (RoleMatrixGate.gs), модули Flowmeter/WorkSchedule сами читают матрицу через `roleMatrixHasPermission`. Диффы с матрицей (осознанные): «ИТР8 pro» + workschedule.view; «ИТР ИОС» + flowmeter.input.

| Роль | Уровень | Доп. права |
|------|---------|-----------|
| `Запрет` | только `dashboard` | — (пользователь лишён доступа) |
| `Общий доступ` | `BASE + CALC` | **Гостевой режим** — не требует входа |
| `ИТР ТОКЕМ` | `BASE + CALC + WHATS_NEW` | + фильтр 4 (ограниченные данные КИП ИОС) |
| `КИП8` | `BASE + CALC + LIB + SECRET + WHATS_NEW` | — |
| `КИП8 pro` | то же, что КИП8 | псевдоним |
| `КИП ИОС` | `BASE + CALC + LIB + KIP_IOS + SECRET + WHATS_NEW` | — |
| `КИП ИОС pro` | то же, что КИП ИОС | + canEdit (каб. журнал) |
| `КИП ИОС дежурный` | `LVL_KIP_IOS_WITH_FLOW` | + расходомеры (просмотр + ввод) |
| `ИТР8` | `LVL_KIP_IOS_WITH_FLOW` | + расходомеры (только просмотр) |
| `ИТР8 pro` | `LVL_KIP_IOS_WITH_FLOW` | псевдоним ИТР8 |
| `ИТР ИОС` | `LVL_KIP_IOS_WITH_FLOW` | + canEdit (каб. журнал) + расходомеры (просмотр) |
| `Админ` | `['*']` (всё) | + админ-панель |

**Уровни доступа (`KipAuth.ROLE_ACCESS`):**
```javascript
_BASE_PAGES       = ['dashboard'];
_CALC_PAGES       = ['calculators', 'calc-kipa', 'calc-electro', 'calc-geometry',
                    'converter', 'conv-*', 'scale-signal', 'error-*',
                    'buoy-*', 'temp-sensors', 'orifice-*', 'circuit-breaker', 'geo-*'];
_LIBRARY_PAGES   = ['docs', 'library', 'library-internal', 'library-electro',
                    'exam-tickets', 'tickets-4', 'tickets-5', 'tickets-6', 'tickets-1000v'];
_KIP_IOS_PAGES   = ['kip-ios', 'devices-*', 'lockouts-*', 'valves-*', 'regulators-*',
                    'projects-*', 'cable-journal-*', 'plan-114', 'plan-114-view'];
_SECRET_PAGES    = ['minesweeper', 'phonebook'];
_WHATS_NEW_PAGES = ['whats-new'];
_FLOWMETER_PAGES = ['flowmeter-data', 'flowmeter-detail'];
_CHARTS_PAGES    = ['charts'];

LVL_OBSHCHIJ            = BASE + CALC;
LVL_KIP8                = BASE + CALC + LIB + SECRET + WHATS_NEW;
LVL_KIP_IOS             = BASE + CALC + LIB + KIP_IOS + SECRET + WHATS_NEW;
LVL_KIP_IOS_WITH_FLOW   = BASE + CALC + LIB + KIP_IOS + FLOWMETER + SECRET + WHATS_NEW;
```

### Полная матрица доступа (по 11 фильтрам)

> Легаси-карта клиента (`KipAuth.ROLE_ACCESS` + фильтры). Источник истины с Task 293-296 — матрица KIP8_Access (см. выше); диффы (осознанные): «ИТР8 pro» + просмотр «Графика работы»; «ИТР ИОС» + ввод показаний расходомеров.

| Роль | 1 Кальк | 2 Библ | 3 КИП ИОС | 4 Огр.КИП ИОС | 5 Запись КЖ | 6 Админ | 7 Секреты | 8 Что нового | 9 Графики | 10 Расход. | 11 Ввод расход. |
|------|---|---|---|---|---|---|---|---|---|---|---|
| Запрет | нет | нет | нет | нет | нет | нет | нет | нет | нет | нет | нет |
| Общий доступ | да | нет | нет | нет | нет | нет | нет | нет | нет | нет | нет |
| ИТР ТОКЕМ | да | нет | нет | да | нет | нет | нет | да | нет | нет | нет |
| КИП8 | да | да | нет | нет | нет | нет | да | да | нет | нет | нет |
| КИП8 pro | да | да | нет | нет | нет | нет | да | да | нет | нет | нет |
| КИП ИОС | да | да | да | нет | нет | нет | да | да | нет | нет | нет |
| КИП ИОС pro | да | да | да | нет | да | нет | да | да | нет | нет | нет |
| КИП ИОС дежурный | да | да | да | нет | нет | нет | да | да | нет | да | да |
| ИТР8 | да | да | да | нет | нет | нет | да | да | нет | да | нет |
| ИТР8 pro | да | да | да | нет | нет | нет | да | да | нет | да | нет |
| ИТР ИОС | да | да | да | нет | да | нет | да | да | нет | да | нет |
| Админ | да | да | да | нет | да | да | да | да | да | да | да |

**Фильтр 9 (Графики)** — только в десктопе (Electron); в PWA скрыт. Виден только Админу.

**Фильтр 4 (Ограниченные данные КИП ИОС)** — скрывает разделы «Графики», «Проекты» и «Кабельный журнал», скрывает столбец «Замечания» в карточках, скрывает приборы без «В гр. ППР» и блокировки без «В перечне». Применяется к роли «ИТР ТОКЕМ».

### Защита на уровне `_applyRoleToUI()`

При любой смене роли:
1. **Sidebar items** — извлекает `page` из `onclick="navigateTo('xxx')"`, сравнивает с `ROLE_ACCESS[role]`, скрывает при отсутствии доступа
2. **Sidebar groups** — если есть `data-requires="library"` / `"kip-ios"`, скрывает всю группу при отсутствии доступа
3. **Dashboard menu buttons** (`.menu-btn`) — аналогично sidebar items
4. **Админ-страницы** — двойная защита: CSS + JS-проверка при `navigateTo()`

### Серверные READ_ROLES / INPUT_ROLES (Flowmeter.gs)

```javascript
READ_ROLES:  ['КИП ИОС дежурный', 'ИТР8', 'ИТР8 pro', 'ИТР ИОС', 'КИП ИОС pro', 'Админ'];
INPUT_ROLES: ['КИП ИОС дежурный', 'Админ'];
```

> ⚠️ **Task 295:** списки выше — legacy-запас (матрица недоступна / частичный деплой). Основная проверка — `flowmeter.view` / `flowmeter.input` из матрицы KIP8_Access (`_requireRead`/`_requireEdit` → `roleMatrixHasPermission`, гейт `rmRequirePerm` в Code.gs).


---

## Ключевые архитектурные решения

### Service Worker (`sw.js`)
- `CACHE_VERSION` (например, `kipia-v411`) + `IMAGE_CACHE_VERSION`
- Стратегия: **NetworkFirst** для `data/*.json` (с cache-busting через `?_t=timestamp`), CacheFirst для App Shell
- При изменении любых файлов приложения — **обязательно** инкрементировать `CACHE_VERSION`, иначе пользователи увидят старую версию

### Навигация — шевроны ‹‹
- 1 тап по шеврону = назад (`goBack()`)
- 2 тапа в течение 0.5 с = на главную (`navigateTo('dashboard')`)

### Изоляция localStorage (только в kip8test)
- Обёртка `isolateLocalStorage()` применяет префикс `kip8test:` ко всем ключам, **кроме** `kip8_*` и `kip8_flow_*` (они общие с kip8 для совместного входа)
- При переносе из kip8test в kip8 этот блок **УДАЛЯЕТСЯ** (см. `scripts/prepare-kip8-transfer.py`)
- В kip8, kip8-desktop, kip8test-desktop этого блока **НЕТ** (десктопы тянут `index.html` из PWA-репозиториев)

### Кэширование данных (ВАЖНО!)
- Все `data/*.json` кэшируются SW. При их изменении — инкрементировать `CACHE_VERSION`
- Динамические данные (кабельный журнал, расходомеры) — через Apps Script, без кэширования SW

### Кросс-ссылки между разделами (фильтр по № проекта)
- Кнопка «Проекты» в карточках открывает `projects-prod?project=N` — фильтрует список по номеру проекта
- Реализовано через URL-параметры + `URLSearchParams`

### Цветовая палитра КИП ИОС
- Приборы: `#b85a7a` (медно-розовый)
- Блокировки: `#b85a7a` (та же палитра)
- Клапана: `#4a8a8c` (бирюзовый)
- Регуляторы: `#7e5ab8` (аметистовый)
- Проекты: `#b08048` (медно-янтарный)
- Кабельный журнал: `#8aa070` (оливково-зелёный)
- План корпуса 114: `#7896b0` (сине-стальной)

### Тёмная и светлая темы
- CSS-переменные в `:root` + `:root[data-theme="dark"]`
- Тема хранится в `kip8test:theme` (в kip8 — без префикса, `theme`)
- Переключатель в sidebar внизу

### Sidebar — структура
- Группы: «КИП и А» (калькуляторы), «Электротехника», «Геометрия», «Экзаменационные билеты», «Библиотека КИП и А», «КИП ИОС», «Расходомеры», «Админ-панель» (только Админ), «Что нового»
- Атрибут `data-requires="library"` / `"kip-ios"` на группах — для группового скрытия при отсутствии доступа
- Внизу: email + роль + кнопка «Выйти» (или «Гость» + «Войти в аккаунт»)


---

## 🔄 Синхронизация данных

### Общая схема

```
Google Sheets (xlsx-экспорт по публичной ссылке)
        ↓ Python sync-*.py (cron в GitHub Actions)
data/*.json (в репозитории, кэшируются SW)
        ↓
PWA читает data/*.json при загрузке (NetworkFirst)
```

### Расписание cron (kip8 + kip8test)

kip8 работает на 2 ч раньше kip8test, чтобы не дублировать запросы к Google Sheets:

| Workflow | kip8 (UTC / Нск) | kip8test (UTC / Нск) |
|----------|------|------|
| sync-cables | 01:00 / 08:00 | 03:00 / 10:00 |
| sync-lockouts | 01:10 / 08:10 | 03:10 / 10:10 |
| sync-valves | 01:20 / 08:20 | 03:20 / 10:20 |
| sync-regulators | 01:30 / 08:30 | 03:30 / 10:30 |
| sync-projects | 01:40 / 08:40 | 03:40 / 10:40 |
| sync-devices | 04:00 / 11:00 | 06:00 / 13:00 |
| update-exam-tickets | 04:10 / 11:10 | 06:10 / 13:10 |

### Двусторонняя синхронизация «клиент ↔ Apps Script ↔ Google Sheets» (паттерн)

Используется для кабельного журнала и расходомеров хозрасчётных:

1. **Google Sheets** — таблица с данными (доступ «у кого есть ссылка» для чтения)
2. **Apps Script — X.gs** — серверный модуль: `list()`, `update*()`, `_requireRead()`, `_requireEdit()`, `audit()`
3. **Apps Script — Code.gs** — роутинг в `doPost()` по action (`x.list`, `x.update*`)
4. **scripts/X.gs** — справочная копия в репозитории (НЕ исполняется)
5. **scripts/sync-x.py** — Python-скрипт для офлайн-fallback (экспорт xlsx → JSON)
6. **data/x.json** — статический fallback для PWA
7. **XData модуль в `index.html`** — клиент: `init()`, `load()`, `renderList()`, `openDetail()`, `submitInput()`, `_canEdit()`

**Серверные готчи:**

- **Даты:** Apps Script получает Date objects через `new Date(year, month-1, day)` (месяцы 0-indexed!). Читать даты нужно через `_sheetToClientDate()` — преобразует Date в M/D/YYYY format. В Excel-форматах используется `datetime_to_serial()` — конвертация Excel-дат в числовые значения.
- **Content-Type:** `text/plain;charset=utf-8` — НЕ `application/json` (см. раздел auth)
- **Авторизация:** `_requireRead()` / `_requireEdit()` — проверка роли по `READ_ROLES` / `INPUT_ROLES`. Методы возвращают `{ok, ...}` — **без throw**.
- **Аудит:** `Utils.audit(user.email, 'X_ACTION', '', '', message)` для каждой операции записи.
- **Оптимистичное обновление UI:** клиент обновляет UI сразу, потом ждёт ответа сервера. При ошибке — откат.

### Чеклист: добавление нового раздела с двусторонней синхронизацией

1. Google Sheets: создать лист, раздать доступ «у кого есть ссылка»
2. Apps Script — X.gs: скопировать шаблон из `Flowmeter.gs`, заменить `SPREADSHEET_ID`, `SHEET_NAME`, `READ_ROLES`, `INPUT_ROLES`, реализовать `list()` / `update*()`. Даты — всегда Date objects.
3. Apps Script — Code.gs: добавить маршруты в `doPost()`
4. Apps Script — Deploy: создать новую версию веб-приложения
5. scripts/X.gs: сохранить справочную копию
6. scripts/sync-x.py: создать Python-скрипт
7. data/x.json: запустить sync, проверить результат
8. index.html — XData: реализовать клиентский модуль
9. index.html — навигация: добавить `XData.init()` в обработчик перехода на страницу
10. sw.js: добавить `'./data/x.json'` в `ASSETS`, инкрементировать `CACHE_VERSION`


---

## Правила работы

### Перед началом работы
1. **Проверь `CACHE_VERSION`** в `sw.js` (должна соответствовать текущей версии — см. заголовок файла)
2. **Запусти тесты** (только для PWA-репозиториев): `cd <repo> && node tests/run-all.js` — ожидается `1433 passed, 0 failed`
3. **Прочитай `worklog.md`** — там последние записи о работе предыдущих агентов

### При внесении изменений
1. **Не ломай тесты** — после каждого изменения запускай `node tests/run-all.js`
2. **Инкрементируй `CACHE_VERSION`** при изменении любых файлов приложения (HTML/CSS/JS/data). Формат: `kipia-test-v455` → `kipia-test-v456` (для kip8test) или `kipia-v393` → `kipia-v394` (для kip8)
3. **НЕ удаляй блок `isolateLocalStorage()`** в kip8test (и НЕ добавляй его в kip8)
4. **НЕ меняй пути в `manifest.json`** — `/kip8/` или `/kip8test/`
5. **НЕ меняй расписание cron** в workflow без явного разрешения пользователя
6. **Комментарии на русском языке**
7. **Соблюдай код-стайл** — vanilla JS, ES6+, mobile-first, без фреймворков
8. **При изменении `data/*.json`** — проверь `git ls-files data/*.json`. Если untracked — `git add`
9. **При добавлении нового раздела КИП ИОС** — следуй паттерну 12 функций (см. выше), добавь запись в `SUBSECTIONS`, инкрементируй кэш, добавь страницу в `_KIP_IOS_PAGES`
10. **Все файлы выкладывать в `/home/z/my-project/download/`**
11. **При изменении auth/ролей** — проверь, что `ROLE_ACCESS` и `_applyRoleToUI()` корректно обрабатывают новую роль/страницу; учти `data-requires` на sidebar-groups

### ⚠️ ОБЯЗАТЕЛЬНО после изменений
1. Указать таблицу файлов для скачивания в конце ответа
2. Указать старую → новую версию кэша

### Перенос в kip8 (из kip8test в kip8)

| Файл | Действие |
|------|----------|
| `index.html` | Скопировать, **УДАЛИТЬ** блок `isolateLocalStorage()` (скриптом `scripts/prepare-kip8-transfer.py`) |
| `tests/` | Без изменений |
| `data/` | Без изменений |
| `images/` | Без изменений |
| `manifest.json` | Заменить пути `/kip8test/` → `/kip8/` |
| `sw.js` | Заменить `kipia-test-v*` → `kipia-v*`, `kipia-images-test-v*` → `kipia-images-v*` |
| `.github/workflows/` | Без изменений (расписание cron уже разнесено) |
| `.well-known/assetlinks.json` | Использовать отдельный файл для kip8 (package `io.github.bloknett_design.twa`) |
| `scripts/*.gs` | Справочные копии серверного кода — синхронизировать при серверных изменениях (Apps Script ОДИН на оба сайта: заменил файлы в редакторе → «Новая версия» развёртывания AKfycbyt… — один деплой на kip8 и kip8test) |

> **Последняя принятая партия:** Tasks 298-306 (коды статусов Т-12/Т-13, мероприятия И/ОБ/ПЗ/ПР/*, таб_№ текстом, отпуск + плановая смена бейджем, одна кнопка «Сформировать») из kip8test@4fc48bc — kipia-v411, паритет тестов 1433/0 (Task 306-перенос, 2026-09-03; Apps Script: WorkSchedule.gs заменить + «Новая версия» AKfycbyt…, если ещё не задеплоен из kip8test — сервер менялся в Tasks 298/303/304/306).

### Перенос в десктопы (kip8test-desktop / kip8-desktop)

- **НЕ переносить вручную!** GitHub Action `sync-to-desktop.yml` (в `kip8test` или `kip8`) при пуше в `index.html` автоматически создаёт коммит в соответствующем десктоп-репозитории
- Правки в `electron/main.js`, `package.json`, `charts-desktop.js`, `devices-table-desktop.js` — делаются **прямо в десктоп-репозитории** (после проверки в `kip8test-desktop`, если это боевой `kip8-desktop`)
- Для релиза десктопа: push тега `v*` → `build-desktop.yml` собирает Win/Linux/macOS + публикует GitHub Release


---

## 🔐 Автономная работа с GitHub (при наличии токена)

### Условия активации режима

Если пользователь передал GitHub PAT (Personal Access Token) — можно работать автономно: клонировать, коммитить, пушить. **Без PAT — только чтение** (clone через https без авторизации).

### Безопасность токена (КРИТИЧНО)

- PAT хранится в `/home/z/.kip_pat` (права 600)
- В git remote URL вставляется временно: `https://bloknett-design:$(cat /home/z/.kip_pat)@github.com/bloknett-design/<repo>.git`
- **ПОСЛЕ ПУША — ОБЯЗАТЕЛЬНО** сбросить URL: `git remote set-url origin https://github.com/bloknett-design/<repo>.git`
- ❌ НЕ оставлять PAT в `.git/config` — он переживает сессию и утечёт
- ❌ НЕ печатать PAT в чат, не логировать, не коммитить
- ✅ PAT у пользователя нужно запрашивать заново в каждом новом чате (не переносится между сессиями)

### Обязательный workflow автономной задачи

1. **Получить PAT** от пользователя (в чате)
2. **Сохранить в `/home/z/.kip_pat`**: `echo 'ghp_...' > /home/z/.kip_pat && chmod 600 /home/z/.kip_pat`
3. **Клонировать/обновить репозиторий**: `git clone https://bloknett-design:$(cat /home/z/.kip_pat)@github.com/bloknett-design/<repo>.git /home/z/my-project/<repo>` (или `git pull` если уже есть)
4. **Сделать изменения** (правки кода, документации и т. д.)
5. **Проверить** локально: тесты, синтаксис, сборка
6. **Заккоммитить**: `git add . && git commit -m "<сообщение>"`
7. **Запушить**: `git push origin main`
8. **СБРОСИТЬ URL** (ОБЯЗАТЕЛЬНО): `git remote set-url origin https://github.com/bloknett-design/<repo>.git`
9. **Удалить PAT из config** (если остался): проверить `git config --get remote.origin.url` — URL не должен содержать токен
10. **Сообщить пользователю** результат: коммит, ссылку на GitHub, что URL сброшен

### Что НЕ делать в автономном режиме

- ❌ Пушить в `kip8` без проверки в `kip8test` (исключение — срочный хотфикс с разрешения пользователя)
- ❌ Менять `manifest.json`, пути, `isolateLocalStorage()` без явного разрешения
- ❌ Форсить push (`git push -f`) без явного разрешения
- ❌ Оставлять PAT в git config после завершения работы

### Git-автор коммитов

При автоматических коммитах (от GitHub Actions) автор: `kip-bot <bot@kip8test.local>` или `github-actions[bot]`. При ручных коммитах из чата — обычный `user.name` / `user.email` из git config.

### Формат коммит-сообщения

```
<type>: <краткое описание> (v<версия кэша>, Task <номер>)

<опционально: детали>
```

Типы: `feat` (новое), `fix` (багфикс), `refactor` (рефакторинг), `docs` (документация), `style` (стиль), `test` (тесты), `chore` (хозяйственное).


---

## 📚 Инструкция для продолжения редактирования в новых чатах

### 1. Что загрузить в новый чат

**Обязательно (минимум):**
- Файл `Системный_промт_для_приложения_КИПиА.md` (этот файл) — задаёт весь контекст
- GitHub PAT (если нужен пуш/коммит) — пользователь должен передать заново

**Желательно (в зависимости от задачи):**
- `worklog.md` — последние записи о работе предыдущих агентов
- Скриншот проблемы (если задача — фикс бага)
- Ссылка на конкретную страницу приложения (если задача — доработка раздела)

### 2. Команды быстрой проверки состояния

```bash
# Проверить, что репозиторий уже клонирован
ls -la /home/z/my-project/<repo>/index.html 2>/dev/null && echo OK || echo NEED CLONE

# Если нет — клонировать (нужен PAT в /home/z/.kip_pat)
git clone https://bloknett-design:$(cat /home/z/.kip_pat)@github.com/bloknett-design/<repo>.git /home/z/my-project/<repo>

# Обновить локальную копию
cd /home/z/my-project/<repo> && git pull origin main

# Текущая версия кэша (для PWA-репо)
grep 'CACHE_VERSION' sw.js

# Запустить тесты (только для PWA)
cd /home/z/my-project/<repo> && node tests/run-all.js
# Ожидается: 1433 passed, 0 failed

# Проверить, что нет незакоммиченных изменений
cd /home/z/my-project/<repo> && git status

# Количество страниц и строк в index.html
grep -c 'id="page-' index.html
wc -l index.html
```

### 3. Где что находится

| Что | Путь |
|------|------|
| Локальные копии репозиториев | `/home/z/my-project/{kip8,kip8test,kip8-desktop,kip8test-desktop}/` |
| `index.html` (весь код PWA) | `/home/z/my-project/<repo>/index.html` |
| Manifest | `/home/z/my-project/<repo>/manifest.json` (только PWA) |
| Данные JSON | `/home/z/my-project/<repo>/data/*.json` |
| Скрипты синхронизации | `/home/z/my-project/<repo>/scripts/sync-*.py` (только PWA) |
| Тесты | `/home/z/my-project/<repo>/tests/` (только PWA) |
| Workflows | `/home/z/my-project/<repo>/.github/workflows/` |
| Изображения | `/home/z/my-project/<repo>/images/` |
| Electron main | `/home/z/my-project/<repo>/electron/main.js` (только десктоп) |
| Системный промт | `/home/z/my-project/<repo>/Системный_промт_для_приложения_КИПиА.md` |
| Worklog | `/home/z/my-project/<repo>/worklog.md` |
| GitHub PAT | `/home/z/.kip_pat` (если передан в чате) |
| Скрипты генерации (вне репо) | `/home/z/my-project/scripts/` |
| Готовые файлы для скачивания | `/home/z/my-project/download/` |

### 4. Что НЕ нужно делать новому чату

- ❌ Заново читать весь `index.html` (~27-30 тыс. строк) — это дорого и бессмысленно. Достаточно `grep` по нужным маркерам (см. ниже)
- ❌ Клонировать репо заново, если уже есть в `/home/z/my-project/<repo>/` — сделай `git pull`
- ❌ Запрашивать у пользователя информацию, которая уже есть в `worklog.md` или в этом промте
- ❌ Менять что-либо в `manifest.json`, путях, `isolateLocalStorage()` без явного разрешения
- ❌ Пушить в `kip8` без проверки в `kip8test` (исключение — срочный хотфикс)

### 5. Карта ключевых мест в `index.html` (для быстрого поиска через grep)

| Что искать | Команда |
|------------|---------|
| CSS-переменные тем | `grep -n ':root' index.html` |
| `.sidebar` CSS | `grep -n '.sidebar {' index.html` |
| `SECRET_BUTTON_IDS` | `grep -n 'SECRET_BUTTON_IDS' index.html` |
| `SUBSECTIONS` реестр | `grep -n 'SUBSECTIONS' index.html` |
| `isolateLocalStorage()` (только kip8test) | `grep -n 'isolateLocalStorage' index.html` |
| HTML login screen | `grep -n 'auth-login-screen' index.html` |
| `KipAuth` модуль | `grep -n 'const KipAuth' index.html` |
| `KipCableJournal` модуль | `grep -n 'KipCableJournal' index.html` |
| `KipFav` модуль | `grep -n 'const KipFav' index.html` |
| `FlowmeterData` модуль | `grep -n 'FlowmeterData' index.html` |
| `ROLE_ACCESS` | `grep -n 'ROLE_ACCESS' index.html` |
| `_applyRoleToUI` | `grep -n '_applyRoleToUI' index.html` |
| `navigateTo` | `grep -n 'function navigateTo' index.html` |
| Bootstrap | `grep -n 'bootstrap' index.html` |
| `WEB_APP_URL` | `grep -n 'WEB_APP_URL' index.html` |

### 6. Типовые задачи и их решение

- **Фикс бага на странице X:** `grep -n 'id="page-X"' index.html` → правки в найденном участке
- **Доработка роли (Task 293-296):** поставить/снять галочку в матрице KIP8_Access (лист matrix) — сервер подхватит ≤ 5 мин (или мгновенно `roleMatrixInvalidateCache()`), клиент — при входе/загрузке/heartbeat ≤ ~10 мин, БЕЗ деплоев и релизов. `ROLE_ACCESS` в index.html и `READ_ROLES`/INPUT_ROLES` в .gs — только легаси-запас (править при смене «аварийной» карты, серверная видимость прав не даёт)
- **Новый раздел КИП ИОС:** следуй чеклисту в разделе «Синхронизация данных»
- **Обновление данных:** запусти `python3 scripts/sync-X.py` — обновится `data/X.json`
- **Сборка десктопа:** push тега `v*` в `kip8test-desktop` или `kip8-desktop`
- **Сборка APK:** только в `kip8` — см. отдельный раздел про APK (через Bubblewrap CLI)


---

## Полезные команды

```bash
# Локальный сервер для разработки (PWA)
cd <repo> && python3 -m http.server 8000
# открыть http://localhost:8000

# Проверка, что manifest.json валиден
python3 -c "import json; json.load(open('manifest.json')); print('OK')"

# Проверка имён кэшей в sw.js
grep 'CACHE_VERSION\|IMAGE_CACHE_VERSION' sw.js

# Проверка префикса localStorage в index.html (только kip8test)
grep -c 'isolateLocalStorage' index.html
# ожидается: >= 2 (определение функции + её вызов)

# Проверка JS-синтаксиса извлечённых блоков из index.html
node --check <(sed -n '/<script>/,/<\/script>/p' index.html | head -n -1 | tail -n +2)

# Ручной запуск sync-скрипта (нужен интернет)
pip install openpyxl requests
python3 scripts/sync-cables.py
# результат: data/cables.json обновится

# Подсчёт страниц в index.html
grep -c 'id="page-' index.html
# ожидается: 93 (включая 3 страницы графика работы)

# Подсчёт строк в index.html
wc -l index.html

# Проверить, что KipAuth модуль на месте
grep -n 'const KipAuth' index.html

# Получить список всех страниц
grep -oE 'id="page-[^"]+"' index.html | sort -u

# Список всех ролей в ROLE_ACCESS
grep -A1 "ROLE_ACCESS = {" index.html | head -20

# Проверить, что изоляция localStorage на месте (только kip8test)
grep -c 'isolateLocalStorage' index.html

# Git: проверить, что нет PAT в URL после пуша
git remote get-url origin | grep -E 'ghp_|github_pat_' && echo "PAT LEAK!" || echo "URL clean"
```


---

## Формат ответов

- Файлы выкладывать в `/home/z/my-project/download/` (или `/home/z/my-project/download/<repo>/` если изменений несколько)
- Новые и обновлённые файлы выкладывать в чат, для возможности скачивания их пользователем
- **ВСЕГДА** указывать какие файлы скачивать и куда (таблица в конце ответа)
- **ВСЕГДА** указывать старую и новую версию кэша (при изменениях в PWA)
- Комментарии в коде — на русском языке
- Не использовать эмодзи в коде или файлах проекта (только в README/промте для наглядности)

